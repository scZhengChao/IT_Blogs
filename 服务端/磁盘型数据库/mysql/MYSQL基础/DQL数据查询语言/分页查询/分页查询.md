# 分页查询

## 目录

- [分页查询语法](#分页查询语法)
- [补充：](#补充)
- [案例](#案例)
  - [查询学生表中数据，跳过前面1条，显示4条](#查询学生表中数据跳过前面1条显示4条)

# 分页查询语法

```sql 
SELECT 字段列表 FROM 表名 LIMIT  起始索引 , 查询条目数;
```


- 起始索引：从0开始,索引是0表示数据表第一行数据。

> 计算公式：起始索引 = (当前页码-1)  \*  每页显示的条数

# 补充：

- 分页查询 `limit` 是MySQL数据库的方言
- Oracle 分页查询使用 `rownumber`
- SQL Server分页查询使用 `top`

# 案例

#### 查询学生表中数据，跳过前面1条，显示4条

```sql 
SELECT * FROM student3 LIMIT 1,4;
```


> 注意：

**如果第一个参数是**\*\*`0`\*\***可以简写：**

```sql 
SELECT * FROM student3 LIMIT 0,5;
SELECT * FROM student3 LIMIT 5;

```


- LIMIT 0，5;  **不够5条，有多少显示多少**
- `limit` 必须书写在`select` **语句的最后；**
