# 指令

## 目录

- [1.指令的挂载](#1指令的挂载)
- [2.指令操作数据](#2指令操作数据)
- [3.指令实际详解](#3指令实际详解)

directives

# **1.指令的挂载**

```typescript 
import changeState from './changeState';

import clipboard from './clipboard';

export  {

  changeState,clipboard

}

import * as directives from '@/directive';

Object.keys(directives).forEach((key) => { Vue.directive(key, directives[key]) })

//---------------------推荐自动挂载全局的公共指令 ---------------------------

const requireDirectives = require.context('@/directives',false,/.js$/)
requireDirectives.keys().forEach(dirName=>{
    const directiveConfig = requireDirectives(dirName)
    const directiveName = dirName.replace(/^.\//,'').replace(/.\w+$/,'')
    Vue.directive(directiveName,directiveConfig.default || directiveConfig)
})
```


# **2.指令操作数据**

    很实用的文章: [https://www.cnblogs.com/xiaojingyuan/p/6555988.html](https://www.cnblogs.com/xiaojingyuan/p/6555988.html "https://www.cnblogs.com/xiaojingyuan/p/6555988.html")

    公共函数的使用和加载: [https://www.cnblogs.com/chase-star/p/9990124.html](https://www.cnblogs.com/chase-star/p/9990124.html "https://www.cnblogs.com/chase-star/p/9990124.html")

- \*\* 通过vnode.context.data 可以直接改data  \*\*​
- **通过 binding.value 传进来函数改变data ,**
- **但是不能通过binding.value来直接操作data，**
- **指令之间的共享数据最好通过dataset 实现**

# **3.指令实际详解**

指令钩子函数会被传入以下参数：

\* el：指令所绑定的元素，可以用来直接操作 DOM。

\* binding：一个对象，包含以下属性：

    \* name：指令名，不包括 v- 前缀。

    \* value：指令的绑定值，**例如：v-my-directive="1 + 1" 中，绑定值为 2。**

    \* oldValue：指令绑定的前一个值\* *，仅在 update 和 componentUpdated 钩子中可用。无论值是否改变都可用。* \*

    \* expression：字符串形式的指令表达式。例如 v-my-directive="1 + 1" 中，表达式为 "1 + 1"。

    \* arg：传给指令的参数，可选。**例如 v-my-directive:foo 中，参数为 "foo"。**

    \* modifiers：一个包含修饰符的对象。例**如：v-my-directive.foo.bar 中，修饰符对象为 { foo: true, bar: true }。**

\* vnode：Vue 编译生成的虚拟节点。移步 VNode API 来了解更多详情。

\* oldVnode：上一个虚拟节点，仅在 update 和 componentUpdated 钩子中可用

\*\*      除了 el 之外，其它参数都应该是只读的，切勿进行修改。如果需要在钩子之间共享数据，建议通过元素的 dataset 来进行。(很重要，要理解)\*\* ​

```typescript 
//五大时机  -------------------------------五大指令时机 顺序,不加时机就是默认bind 和 update是调用------------
focus:{

     bind:function(el,binding，vnode,oldVnode){ //最先
         console.log('指令第一次绑定到元素时调用')
     },
     inserted(){//其次
        console.log(this) //什么时期之后再执行,相当于回调,this只想window
        console.log('绑定指令的元素插入到父节点时调用') 
    },
    update(){//跟新
        console.log('指令所在的元素的model层的数据，view有更新请求时') 
    },
    componentUpdated(){//跟新完成时
         console.log('更新完成时') 
    },
    unbind(el,binding,newVnode,oldValue){ // 指令解绑( 组件destoryed 的时候触发)

    }
}
```


[内置指令](内置指令.md "内置指令")

[自定义指令二](自定义指令二.md "自定义指令二")

[自定义指令 一](<自定义指令 一.md> "自定义指令 一")
