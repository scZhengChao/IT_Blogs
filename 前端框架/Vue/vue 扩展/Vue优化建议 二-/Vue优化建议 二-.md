# Vue优化建议 二:

```纯文本 
 1.数据可响应 
     Vue.observable( object ) 
 const state = Vue.observable({ count: 0 }) 
 在 Vue 2.x 中，被传入的对象会直接被 Vue.observable 改变，所以如这里展示的， 它和被返回的对象是同一个对象 。在 Vue 3.x 中，则会返回一个可响应的代理， 而对源对象直接进行修改仍然是不可响应的。 因此，为了向前兼容， 我们推荐始终操作使用 Vue.observable 返回的对象，而不是传入源对象。
```


```纯文本 
 2.computed 很好用 
     vue中computed计算属性可以用来绑定动态变量，而且它可以实时刷新，并且也可以在watch中监控，所以本人很喜欢用这个东西，一般把需要随data中变量一起变化的属性全部放在这里面，data中只定义互相独立互相不影响的变量，这样可以极大地减少重复变量的定义，使代码结构清晰，也便于排查错误。 
      大概就是 如果你想一个对象的变化引起 computed的执行; 麻烦你要事先设定这个对象的key; 而不是动态添加; 动态添加或者改变不会引起computed的执行; 数组类似; 不能改变引用,改变了引用也不能变化;  还需在研究 
     而且 他只关联同步的 data; 异步的data可以改变;但是他不关联;异步改变的data必须在同步里出现过
```


**3。申明响应式属性**

由于 Vue 不允许动态添加根级响应式属性，所以你必须在初始化实例前声明所有根级响应式属性，

![  ](607f3da51d9525ee660801a0209d32bb_9AWnLJ8lla.png "  ")

**4.this.forceUpdate()   强制刷新**

如果你发现你自己需要在 Vue 中做一次强制更新，99.9% 的情况，是你在某个地方做错了事。

你可能还没有留意到[**数组**](https://cn.vuejs.org/v2/guide/list.html#%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9 "数组")或[**对象**](https://cn.vuejs.org/v2/guide/list.html#%E5%AF%B9%E8%B1%A1%E5%8F%98%E6%9B%B4%E6%A3%80%E6%B5%8B%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9 "对象")的变更检测注意事项，或者你可能依赖了一个未被 Vue 的响应式系统追踪的状态。

然而，如果你已经做到了上述的事项仍然发现在极少数的情况下需要手动强制更新，那么你可以通过[**\$forceUpdate**](https://cn.vuejs.org/v2/api/#vm-forceUpdate "\$forceUpdate")来做这件事。
