# 运行时配置

## 目录

- [alias](#alias)
- [全局mixin.less](#全局mixinless)
- [修改全局less变量](#修改全局less变量)
- [autoprefixer](#autoprefixer)

# alias

配置别名，对引用路径进行映射。

```bash 
export default {
  alias: {
    foo: '/tmp/a/b/foo',
  },
};
```


然后 `import('foo')`，实际上是 `import('/tmp/a/b/foo')`。

Umi 内置了以下别名：

- `@`，项目 src 目录
- `@@`，临时目录，通常是 `src/.umi` 目录
- `umi`，当前所运行的 umi 仓库目录
- `react-router` 和 `react-router-dom`，底层路由库，锁定版本，打包时所有依赖了他们的地方使用同一个版本
- `react` 和 `react-dom`，默认使用 `16.x` 版本，但如果项目里有依赖，会优先使用项目中依赖的版本

# 全局mixin.less

直接引入到global.less 貌似不生效；不知道为啥；

同样有问题；会导致theme变量失效

```typescript title="config.ts"
lessLoader: {
    modifyVars: {
      hack: 'true; @import "~@/styles/mixin.less";',
    },
},

```


mixin路径

![](image_-nlTODzmjV.png)

# 修改全局less变量

[修改变量](<修改变量.md> "修改变量")

[ 如何在 umi 系项目中实现动态换肤 - 掘金 我所知道的动态换肤方案有两种，一种是通过使用 less.modifyVars 修改 less 变量实现，一种是使用 var css 实现，由于 var css 很多浏览器都不支持。 而且我们项目的主要场景在于移动端，所以能选择的就只有通过 less 实现的方案了。 原理其实很好… https://juejin.cn/post/6922733510348242957](https://juejin.cn/post/6922733510348242957 " 如何在 umi 系项目中实现动态换肤 - 掘金 我所知道的动态换肤方案有两种，一种是通过使用 less.modifyVars 修改 less 变量实现，一种是使用 var css 实现，由于 var css 很多浏览器都不支持。 而且我们项目的主要场景在于移动端，所以能选择的就只有通过 less 实现的方案了。 原理其实很好… https://juejin.cn/post/6922733510348242957")

# autoprefixer

- 类型：`object`
- 默认值：`{ flexbox: 'no-2009' }`

用于解析 CSS 并使用来自 Can I Use 的值将供应商前缀添加到 CSS 规则。如自动给 CSS 添加 `-webkit-` 前缀。

更多配置，请查阅 [*autoprefixer 的配置项*](https://github.com/postcss/autoprefixer#options "autoprefixer 的配置项")*。*

```react tsx 
  import autoprefixer  from 'autoprefixer'
  defineConfig({
      extraPostCSSPlugins: [
        autoprefixer({remove:false})
      ],
     targets: {
        ie: 11,
        Chrome：104，
     },
  })
  
 //package.json 
"browserslist": [
    "> 1%",
    "last 2 versions"
]
```


**一定要引入；  不引入不行**；注**意版本对应**  postcss 8.4  对应 autoprefixer 9.0.0&#x20;

[ -webkit-box-orient:vertical 编译报错之autoprefixer问题 由于各大浏览器的兼容问题，autoprefixer\[https://www.npmjs.com/package/autoprefixer\] 插件 就可以帮我们自动补齐前缀。它... https://www.jianshu.com/p/14625f897ea8](https://www.jianshu.com/p/14625f897ea8 " -webkit-box-orient:vertical 编译报错之autoprefixer问题 由于各大浏览器的兼容问题，autoprefixer\[https://www.npmjs.com/package/autoprefixer] 插件 就可以帮我们自动补齐前缀。它... https://www.jianshu.com/p/14625f897ea8")

- `display: -webkit-box;` 将对象作为弹性伸缩盒子模型显示。
- `-webkit-line-clamp: 2;` 这个属性**不是css的规范属性**，需要组合上面两个属性，表示显示的行数。
- `-webkit-box-orient: vertical;` 从上到下垂直排列子元素（设置伸缩盒子的子元素排列方式）

```react tsx 
/* autoprefixer: ignore next */
-webkit-box-orient: vertical;

.clamp_fun(@line: 1) {
  overflow: hidden;
  text-overflow: ellipsis;
  /* autoprefixer: off*/
  -webkit-box-orient: vertical;
  /* autoprefixer: on*/
  display: -webkit-box;
  -webkit-line-clamp: @line;
}

.clamp_1 {
  .clamp_fun(1);
}
.clamp_2 {
  .clamp_fun(2);
}
.clamp_3 {
  .clamp_fun(3);
}

```


[包装根节点](包装根节点.md "包装根节点")
