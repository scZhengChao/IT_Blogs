# Url 处理

## 目录

- [Qs](#Qs)

处理url

[npm: url The core \`url\` packaged standalone for use with Browserify. https://www.npmjs.com/package/url](https://www.npmjs.com/package/url "npm: url The core `url` packaged standalone for use with Browserify. https://www.npmjs.com/package/url")

# Qs

qs.parse()

qs.stringify()

处理参数

[npm: qs A querystring parser that supports nesting and arrays, with a depth limit https://www.npmjs.com/package/qs](https://www.npmjs.com/package/qs "npm: qs A querystring parser that supports nesting and arrays, with a depth limit https://www.npmjs.com/package/qs")
