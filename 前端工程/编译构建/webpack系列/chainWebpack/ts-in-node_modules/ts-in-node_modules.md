# ts-in-node\_modules

## 目录

- [umi:](#umi)

# umi:

[   https://github.com/umijs/umi/issues/8569](https://github.com/umijs/umi/issues/8569 "   https://github.com/umijs/umi/issues/8569")

```javascript 
config.module
      .rule('ts-in-node_modules')
      .test(/\.(js|jsx|ts|tsx)$/)
      .exclude.add(require('path').join(__dirname, '../node_modules'))
      .end()
      .include.clear()
      .add(require('path').join(__dirname, '../../../packages'));
```


***

[   https://github.com/umijs/umi/issues/4834](https://github.com/umijs/umi/issues/4834 "   https://github.com/umijs/umi/issues/4834")

```javascript 
Module parse failed: Unexpected token (21:2)
You may need an appropriate loader to handle this file type, currently no loaders are configured to process this file. See https://webpack.js.org/concepts#loaders
|
| const PageLoading = () => (
>   <div style={style}>
|     <Spin size="large" />
|   </div>

 @ ./src/layouts/UserLayout.jsx 9:0-46
 @ ./src/.umi/core/routes.ts
 @ ./src/.umi/umi.ts
 @ multi ./node_modules/@umijs/bundler-webpack/lib/getConfig/runtimePublicPathEntry.js ./src/.umi/umi.ts
```


原因是引入当前项目外的组件报错

```javascript 
import Loading from '../../../../../../../PageLoading'

```


当把 PageLoading 组件移动到当前项目的 src 下就正常 ， 这要怎么处理？

如果是 ts 文件，可以用以下配置，

```javascript 
chainWebpack(memo) {
  memo.module.rule('ts-in-node_modules').include.clear();
  return memo;
},
```


From what I understand, you're trying to transpile modules in `node_modules`. I have the same issue and I'm trying to find a solution.

> 据我所知，您正试图在node\_modules中转换模块。我也有同样的问题，我正在努力寻找解决方案。

或者

```javascript 
chainWebpack(memo) {
  memo.module.rule('js').include.clear();
  return memo;
}
```
