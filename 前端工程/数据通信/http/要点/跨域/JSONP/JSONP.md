# **JSONP**

**JSONP(JSON with Padding)，前端+后端方案，绕过跨域**
&#x20;   前端构造script标签请求指定URL（**由script标签发出的GET请求不受同源策略限制**），服务器**返回一个函数执行语句**，该函数**名称通常由查询参callback的值决定**，**函数的参数为服务器返回的json数据。** 该函数在前端执行后即可获取数据。
