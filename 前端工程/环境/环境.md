# 环境

[cross-env](cross-env.md "cross-env")

[环境变量](IT/前端工程/环境/环境变量/环境变量.md "环境变量")

[(运行时)加载不同的js 文件](<(运行时)加载不同的js 文件.md> "(运行时)加载不同的js 文件")

[(build time)加载不同的js文件](<(build time)加载不同的js文件.md> "(build time)加载不同的js文件")
