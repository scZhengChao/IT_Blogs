# 特性

1. **哈希函数特性**：
   - **确定性**：相同输入永远产生相同输出
   - **雪崩效应**：微小改动会导致哈希值巨大变化
   - **固定长度**：无论输入多大，输出总是128位(32字符)哈希值
2. **唯一性保证机制**：
   - 理论上两个不同文件有相同MD5的概率极低(约1/2^128)
   - 实际应用中可认为：MD5不同 → 文件必定不同；MD5相同 → 文件极大概率相同
