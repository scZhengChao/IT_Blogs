# getCell

## 目录

- [参数](#参数)
- [返回值](#返回值)

`evt.getCell`方法用于获取触发事件的单元格对象（`mxCell`）。`mxCell`是`mxGraph`中表示图形元素（如节点、边）的基本对象，包含了单元格的属性和数据。

#### 参数

该方法不接收任何参数。

#### 返回值

如果事件是由某个单元格触发的，`evt.getCell`方法会返回对应的`mxCell`对象。该对象包含了单元格的一些基本属性，例如单元格的值、ID 等。

如果事件不是由单元格触发的，则返回`null`。
