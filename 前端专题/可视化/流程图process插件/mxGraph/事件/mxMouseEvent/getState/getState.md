# getState

`evt.getState`方法会返回一个`mxCellState`对象。该对象包含以下一些重要属性：

- **`cell`**：指向对应的`mxCell`对象。
- **`x`****和****`y`**：单元格在视图中的左上角坐标。
- **`width`****和****`height`**：单元格的宽度和高度。
- **`style`**：应用于该单元格的样式对象。

如果事件不是由单元格触发的，或者该单元格没有对应的状态信息，则返回`null`。
