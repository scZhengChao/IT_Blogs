# 钩子-生命周期

## 目录

- [Description](#Description)
- [Pre & Post Scripts](#Pre--Post-Scripts)
- [Life Cycle Scripts](#Life-Cycle-Scripts)
- [Life Cycle Operation Order](#Life-Cycle-Operation-Order)
  - [npm publish](#npm-publish)
  - [npm pack](#npm-pack)
  - [npm install](#npm-install)
  - [npm start](#npm-start)
- [demo](#demo)

[   https://docs.npmjs.com/cli/v6/using-npm/scripts#pre--post-scripts](https://docs.npmjs.com/cli/v6/using-npm/scripts#pre--post-scripts "   https://docs.npmjs.com/cli/v6/using-npm/scripts#pre--post-scripts")

### Description

The `"scripts"` property of of your `package.json` file supports a number of built-in scripts and their preset life cycle events as well as arbitrary scripts. These all can be executed by running `npm run-script <stage>` or `npm run <stage>` for short. *Pre* and *post* commands with matching names will be run for those as well (e.g. `premyscript`, `myscript`, `postmyscript`). Scripts from dependencies can be run with `npm explore <pkg> -- npm run <stage>`.

> package.json文件中的 "scripts "属性支持一些内置的脚本和它们预设的生命周期事件，以及任意的脚本。这些都可以通过运行npm run-script \<stage>或简称npm run \<stage>来执行。名称匹配的前和后命令也会被运行（例如premyscript、myscript、postmyscript）。来自依赖的脚本可以用npm explore \<pkg> -- npm run \<stage>运行。

### Pre & Post Scripts

To create "pre" or "post" scripts for any scripts defined in the `"scripts"` section of the `package.json`, simply create another script *with a matching name* and add "pre" or "post" to the beginning of them.

> 要为package.json的 "scripts "部分定义的任何脚本创建 "pre "或 "post "脚本，只需创建另一个名称相同的脚本，并在它们的开头加上 "pre "或 "post"。

### Life Cycle Scripts

There are some special life cycle scripts that happen only in certain situations. These scripts happen in addtion to the "pre" and "post" script.

> 有一些特殊的生命周期脚本，只在某些情况下发生。这些脚本是在 "前 "和 "后 "脚本之外发生的。

- `prepare`, `prepublish`, `prepublishOnly`, `prepack`, `postpack`

### Life Cycle Operation Order

#### [npm publish](https://docs.npmjs.com/cli/v6/commands/npm-publish "npm publish")

- `prepublishOnly`
- `prepare`
- `prepublish`
- `publish`
- `postpublish`

#### [npm pack](https://docs.npmjs.com/cli/v6/commands/npm-pack "npm pack")

- `prepack`
- `postpack`

#### [npm install](https://docs.npmjs.com/cli/v6/commands/npm-install "npm install")

- `preinstall`
- `install`
- `postinstall`

**Also triggers**

- `prepublish` (when on local)
- `prepare` (when on local)

#### [npm start](https://docs.npmjs.com/cli/v6/commands/npm-start "npm start")

`npm run start` has an `npm start` shorthand.

- `prestart`
- `start`
- `poststart`

# demo

接着我们来看 [preinstall](https://link.juejin.cn/?target=https://github1s.com/vuejs/vue-next/blob/HEAD/scripts/preinstall.js#L1-L8 "preinstall") 源码。  只允许使用npm

```typescript 
// vue-next/scripts/preinstall.js

if (!/pnpm/.test(process.env.npm_execpath || '')) {
  console.warn(
    `\u001b[33mThis repository requires using pnpm as the package manager ` +
      ` for scripts to work properly.\u001b[39m\n`
  )
  process.exit(1)
}
```


[prepare](prepare.md "prepare")
