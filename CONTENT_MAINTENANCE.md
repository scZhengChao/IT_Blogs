# 文档目录维护

每个包含文章的目录都必须提供 `index.md`（或 `index.mdx`）。目录入口应链接到直接子目录及独立文章；文章正文可继续作为该目录的首页，不需要额外复制正文。

- 指向目录时使用 `./子目录/index.md`，避免 `./子目录/`。Rspress 对相对目录链接的转换可能产生不存在的 `子目录.html`。
- 图片、PDF、压缩包、代码示例等附件放在对应文章附近的 `assets/` 下，包括作为下载附件的 Markdown 文件。
- 目录名表达主题，完整文章标题保留在正文；避免标题截断、首尾空格及多余的“学习／基础／专题”包装层。
- 内容路径不得包含 `!`、`'`、`?`、`#`；目录不能以 `.d.ts` 结尾或使用 `_` 加非 `_` 字符开头，避免被路由规则排除。
- 移动文章时同步修复站内链接及相对附件路径，并更新 `scripts/content-redirects.json` 中的旧地址映射。

## 检查

使用项目要求的 Node.js 版本（当前验证版本：22.22.2）。

```bash
npm run check:docs
npm run build
node scripts/check-docs.mjs --built
```

`dev` 和 `build` 启动前自动检查目录入口、索引子项完整性、站内目标、保留字符、路由冲突及附件位置。构建期间继续使用 Rspress 原生页面死链检查；附件由本地检查器验证，因为它们不属于页面路由。

`--built` 额外验证每篇文档、被引用附件及旧地址跳转对应的输出文件是否存在。检查不请求外部网站，也不检查章节锚点或下载附件内部的链接。

## 旧地址兼容

`scripts/rspress-content-redirects.ts` 在生产构建完成后生成独立 HTML 跳转页，保留查询参数和章节锚点。跳转映射不进入客户端路由表或搜索索引。

跳转页仅存在于 `doc_build/`，开发服务器不提供这些旧地址。站内链接必须指向新地址；发布时需要部署完整 `doc_build/`。这些是静态 HTML 跳转，不是服务器端 HTTP 301/308。

## 本次整理

- 修复 124 个缺少入口的目录：109 篇原文章改为 `index.md`，15 个目录新增入口页。
- 修复历史 `IT/...`、旧“目录名.md”引用及相对目录链接，补充索引遗漏的子项。
- 精简 Java、mxGraph、Electron、Nest、React Hooks、RxJS 等目录层级，将大量截断的标题目录改为主题名；目录最大深度由 9 层降至 7 层。
- 保留原有 5,375 篇文章及 4,446 个附件。缺失的历史附件或笔记引用改为文字说明，未创建虚假的替代内容。
- `typings.d.ts` 目录改为“类型声明”，避免被框架默认排除规则忽略。

## 验证结果（2026-09-20）

- 生产构建通过，生成 5,390 篇文档及 1,648 个静态跳转页。
- 5,351 个内容目录均有入口，10,081 处本地引用及索引子项检查通过。
- 扫描页面 HTML 中的 278,274 处本地引用，16,463 个不同目标均有对应输出文件。
- 原有 5,375 篇文章均有对应文件，4,446 个附件迁移前后 SHA-256 一致。
- 浏览器验证定时器目录和 React 调度文章可访问，旧文章地址跳转保留查询参数及章节锚点。

## 图片优化（2026-09-21）

- 使用 `npm run optimize:images` 将 3,987 张 PNG 转换为 WebP（质量 82，透明通道质量 100），转换时校验格式、尺寸及动画帧数。
- 图片由 1,109,870,610 字节降至 157,891,654 字节，减少 951,978,956 字节（85.77%）。
- 更新 1,335 篇 Markdown 的 3,992 处本地图片引用；代码示例和外部 PNG URL 保持原样。
- 文档源目录由约 1.3 GB 降至 462 MB；生产产物由约 2.8 GB 降至 1,045,489,057 字节（997.06 MiB）。
- 全量文档检查及生产构建通过，产物中无 PNG；浏览器验证普通路径、含空格路径及透明 WebP 均可正常加载和解码。
