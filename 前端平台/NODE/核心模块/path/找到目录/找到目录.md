# 找到目录

## 目录

- [dirname方法的作用](#dirname方法的作用)

### dirname方法的作用

&#x20;        `path.dirname()`方法接受一个**文件路径**作为参数，返回该路径的**目录名部分**。该方法可以很容易地**将文件路径**从**文件名中分离出来**。例如，如果给定路径为"/foo/bar/baz.txt"，则返回"/foo/bar"。&#x20;

下面是path.dirname()方法的基本语法：

```javascript 
path.dirname(path)
```
