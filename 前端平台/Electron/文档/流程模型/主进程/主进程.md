# 主进程

每个 `Electron` 应用都有一个**单一的主进程**，作为**应用程序的入口点。** 主进程在 `Node.js` 环境中运行，这意味着它具有 `require` 模块和**使用所有 Node.js API 的**能力。

[窗口管理](窗口管理.md "窗口管理")

[应用程序生命周期](应用程序生命周期.md "应用程序生命周期")

[原生 API](<原生 API.md> "原生 API")
