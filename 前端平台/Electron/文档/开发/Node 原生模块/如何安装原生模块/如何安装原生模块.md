# 如何安装原生模块

有多种不同的方法来安装原生模块：

[为 Electron 安装并重新编译模块](<为 Electron 安装并重新编译模块.md> "为 Electron 安装并重新编译模块")

[通过 npm 安装](<通过 npm 安装.md> "通过 npm 安装")

[为 Electron 手动编译](<为 Electron 手动编译.md> "为 Electron 手动编译")

[为Electron的自定义编译手动编译](为Electron的自定义编译手动编译.md "为Electron的自定义编译手动编译")
