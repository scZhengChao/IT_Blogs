# 模板数组菜单和应用程序菜单

`Menu.buildFromTemplate(template)`从模板数组创建一个菜单。

- `template`：一个包含菜单项的数组。每个菜单项都是一个对象，包含 label、click 等属性。

`Menu.setApplicationMenu(menu)`设置应用程序菜单，通常用于顶级菜单栏。

- `menu`：要设置为应用程序菜单的 Menu 对象。
