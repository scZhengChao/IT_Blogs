# ?&#x20;

## 目录

- [?](#)
- [贪婪模式](#贪婪模式)
- [正向先行断言x(?=y)](#正向先行断言xy)
  - [ 实现一个密码强度校验](#-实现一个密码强度校验)
- [反向先行断言(?!表达式)](#反向先行断言表达式)
- [正向后行断言(?<=表达式)](#正向后行断言表达式)
- [反向后行断言(?\<!表达式)](#反向后行断言表达式)
- [总结](#总结)

### ?

? 匹配前面的子表达式**零次或一次**，或**指明一个非贪婪限定符**。**要匹配 ? 字符，请使用 \\?。**

### 贪婪模式

1. .+**表示用贪婪模式进行匹配**\*\*：**/b\w+/g**匹配结果：\*\*b be bee beer beers beeeee
2. **.+?表示用非贪婪模式(懒惰模式)进行匹配**/b\w+?/g匹配结果：b be bee beer beers beeeee
3. 贪婪模式和懒惰模式：  **贪婪模式**就是在满足表达式的前提下，匹配尽可能多的字符；  **懒惰模式**，又称**非贪婪模式**，表示在满足表达式的前提下，匹配尽可能少的字符。

### 正向先行断言x(?=y)

正向先行断言：`(?=表达式)`，指在某个位置往右看，所在的位置右侧必须匹配`表达式`。

1. x(?=y)：仅匹配被y跟随的x；例如，/bruce(?=wayne)/，如果"bruce"后面跟着wayne，则匹配之。/bruce(?=wayne|banner)/ ，如果"bruce"后面跟着"wayne"或者banner，则匹配之。但是，"wayne" 和 "banner" 都不会在匹配结果中出现

#### &#x20;实现一个密码强度校验

> 至少有一个大写字母。至少有一个小写字母。至少有一个数字。至少有 8 个字符。

```javascript 
/(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z]).{8,}/g
```


### 反向先行断言`(?!表达式)`

反向先行断言：`(?!表达式)`，指在某个位置往右看，不能存在`表达式`中的内容。

匹配'x'仅仅当'x'后面不跟着'y',这个叫做正向否定查找。 例如，/\d+(?!\\.)/匹配一个数字仅仅当这个数字后面没有跟小数点的时候。

```javascript 
 /\d+(?!\.)/.exec('3.141') // 匹配 `"141"`，而不是 `"3.141"  
```


## 正向后行断言`(?<=表达式)`

正向后行断言：`(?<=表达式)`，指在某个位置往左看，存在`表达式`中的内容。

![](image_p34V0Nxj1Q.png)

## 反向后行断言`(``?<!表达式)`

反向后行断言：`(``?<!表达式)`，指在某个位置往左看，不能存在`表达式`中的内容。

![](image_sPw7_phA_S.png)

# 总结

匹配例句：1pt 2px 3em 4px

1.可以用(?=ABC)、(?!ABC)的表达式，表示前瞻判断：

1. 正向前瞻判断，示例如下：  **表示在字符"px"前的数字：**/\d(?=px)/g**匹配结果：** 1pt 2px 3em 4px  **说明：** 此处的(?=px)表示一个占位符，而非具体的字符
2. 负向前瞻判断，示例如下：  **表示不在字符"px"前的数字**：  /\d(?!px)/g**匹配结果：** 1pt 2px 3em 4px  **说明：** 此处的(?!px)表示一个占位符，而非具体的字符

2.可以用(?<=ABC)、(?\<!ABC)的表达式，表示回顾判断：

1. 正向回顾判断，示例如下：  **表示在字符"p"后的罗马字符：**/(?<=p)\w/g**匹配结果：** 1pt 2px 3em 4px
2. 负向回顾判断，示例如下：  **表示不在字符"p"后的罗马字符：**/(?\<!p)\w/g**匹配结果：** 1pt 2px 3em 4px
