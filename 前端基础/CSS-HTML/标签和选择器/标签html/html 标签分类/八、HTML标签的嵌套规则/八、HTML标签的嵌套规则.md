# 八、HTML标签的嵌套规则

HTML标签有严格的嵌套规则，以下是一些常见规则：

1. **块级元素可以包含行内元素和其他块级元素**
2. **行内元素不能包含块级元素**
3. **某些元素有特定的内容模型（如**\*\*`<ul>`****只能包含****`<li>`）\*\*​
4. **某些元素不能互相嵌套（如**\*\*`<a>`****不能嵌套****`<a>`）\*\*​
5. **空元素（如**\*\*`<img>`****,****`<br>`）不能包含内容\*\*​
