# 语法

## 目录

- [语法定义](#语法定义)
- [3d](#3d)
- [动画](#动画)

### 语法定义

创建 CSS 动画序列，需要使用 `animation` 属性或 其子属性 `animation-*`，该属性允许配置动画时间、时长以及其他动画细节，但该属性不能配置动画的实际表现，动画的实际表现是由 `@keyframes` 规则实现。

# 3d

```css 
舞台元素概念:stage : 
transform-style:preserve-3d 开启3d   perspective:600px/800px 视角距离
perspective-origin:left/right/center  top/bottom/middle  视角的位置
backface-visibility:hidden/visible  背包隐藏还是可见
```


# 动画

```css 
关键帧动画
@keyframes name {
    0%{}  ==> from {}
    100%{} ==> to{}  (当然,百分比还可以继续细分)
}
animation {
  name 2s linear/ease(或者 step(5) 自动划分为5次结束) delay infinife/number(无限次) alternate(反向)
}


```


[animation-\*](animation--.md "animation-*")

[@keyframe](@keyframe.md "@keyframe")

[语法范例](语法范例.md "语法范例")

[实验性新属性](实验性新属性.md "实验性新属性")

[js监听](js监听.md "js监听")
