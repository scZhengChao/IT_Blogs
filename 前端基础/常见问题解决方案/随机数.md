

## 目录

- [生成uuid 的四种方法](#生成uuid的四种方法)
  - [算法1：](#算法1)
  - [算法2：](#算法2)
  - [算法3：](#算法3)
  - [算法4：](#算法4)
  - [推荐](#推荐)
  - [高端](#高端)
    - [随机颜色生成](#随机颜色生成)
    - [颜色格式转换](#颜色格式转换)

# **生成uuid 的四种方法**

[JS生成 UUID的四种方法\_mr\_raptor的专栏-CSDN博客\_js uuid 全局唯一标识符（GUID，Globally Unique Identifier）也称作 UUID(Universally Unique IDentifier) 。GUID是一种由算法生成的二进制长度为128位的数字标识符。GUID 的格式为“xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx”，其中的 x 是 0-9 或 a-f 范围内的一个32位十六进制数。在理想情况下.. https://blog.csdn.net/mr\_raptor/article/details/52280753](https://blog.csdn.net/mr_raptor/article/details/52280753 "JS生成 UUID的四种方法_mr_raptor的专栏-CSDN博客_js uuid 全局唯一标识符（GUID，Globally Unique Identifier）也称作 UUID(Universally Unique IDentifier) 。GUID是一种由算法生成的二进制长度为128位的数字标识符。GUID 的格式为“xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx”，其中的 x 是 0-9 或 a-f 范围内的一个32位十六进制数。在理想情况下.. https://blog.csdn.net/mr_raptor/article/details/52280753")

    全局唯一标识符（GUID，Globally Unique Identifier）也称作 UUID(Universally Unique IDentifier) 。

    GUID是一种由算法生成的二进制长度为128位的数字标识符。GUID 的格式为“xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx”，其中的 x 是 0-9 或 a-f 范围内的一个32位十六进制数。在理想情况下，任何计算机和计算机集群都不会生成两个相同的GUID。

    GUID 的总数达到了2^128（3.4×10^38）个，所以随机生成两个相同GUID的可能性非常小，但并不为0。GUID一词有时也专指微软对UUID标准的实现。

## 算法1：

```javascript 
 function uuid() {
    var s = [];
    var hexDigits = "0123456789abcdef";
    for (var i = 0; i < 36; i++) {
        s[i] = hexDigits.substr(Math.floor(Math.random() * 0x10), 1);
    }
    s[14] = "4";  // bits 12-15 of the time_hi_and_version field to 0010
    s[19] = hexDigits.substr((s[19] & 0x3) | 0x8, 1);  // bits 6-7 of the clock_seq_hi_and_reserved to 01
    s[8] = s[13] = s[18] = s[23] = "-";
    var uuid = s.join("");
    return uuid;
}
```


## 算法2：

```javascript 
 function guid() {
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
        var r = Math.random()*16|0, v = c == 'x' ? r : (r&0x3|0x8);
        return v.toString(16);
    });
}
```


## 算法3：

```javascript 
function guid() {
   function S4() {
       return (((1+Math.random())*0x10000)|0).toString(16).substring(1);
    }
    return (S4()+S4()+"-"+S4()+"-"+S4()+"-"+S4()+"-"+S4()+S4()+S4());
}


```


## 算法4：

```javascript 
 function uuid(len, radix) {
    var chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz'.split('');
    var uuid = [], i;
    radix = radix || chars.length;
    if (len) {
      // Compact form
      for (i = 0; i < len; i++) uuid[i] = chars[0 | Math.random()*radix];
    } else {
      // rfc4122, version 4 form
      var r;
      // rfc4122 requires these characters
      uuid[8] = uuid[13] = uuid[18] = uuid[23] = '-';
      uuid[14] = '4';
      // Fill in random data.  At i==19 set the high bits of clock sequence as
      // per rfc4122, sec. 4.1.5
      for (i = 0; i < 36; i++) {
        if (!uuid[i]) {
          r = 0 | Math.random()*16;
          uuid[i] = chars[(i == 19) ? (r & 0x3) | 0x8 : r];
        }
      }
    }
    return uuid.join('');
}
// 8 character ID (base=2)
uuid(8, 2)  //  "01001010"
// 8 character ID (base=10)
uuid(8, 10) // "47473046"
// 8 character ID (base=16)
uuid(8, 16) // "098F4D35"
```


## 推荐

第四种 + 时间戳

```react tsx 
export function generateIdUtil(len: number, radix: number = 16) {
  const chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz'.split('');
  const uuid = [];
  let i;
  const radixNum = radix || chars.length;
  if (len) {
    for (i = 0; i < len; i++) uuid[i] = chars[0 | (Math.random() * radixNum)];
  } else {
    let r;
    uuid[8] = uuid[13] = uuid[18] = uuid[23] = '-';
    uuid[14] = '4';
    for (i = 0; i < 36; i++) {
      if (!uuid[i]) {
        r = 0 | (Math.random() * 16);
        uuid[i] = chars[i == 19 ? (r & 0x3) | 0x8 : r];
      }
    }
  }
  return `${uuid.join('')}${String(Date.now()).substr(-8)}`;
}

```


## 高端

[ 打造极致性能的前端uuid生成器 uuid生成，是大家常见的一个业务场景，也有很多现有的库去支持。但是大多都是基于随机数或者是hash算法实现，这也就存在一个尴尬的问题 — 一定极小概率会重复。虽然某些人可能工作几年都没遇到过。但是一旦遇到就… https://zhuanlan.zhihu.com/p/438200185?spm=wolai.workspace.0.0.1464c92fIfF20U](https://zhuanlan.zhihu.com/p/438200185?spm=wolai.workspace.0.0.1464c92fIfF20U " 打造极致性能的前端uuid生成器 uuid生成，是大家常见的一个业务场景，也有很多现有的库去支持。但是大多都是基于随机数或者是hash算法实现，这也就存在一个尴尬的问题 — 一定极小概率会重复。虽然某些人可能工作几年都没遇到过。但是一旦遇到就… https://zhuanlan.zhihu.com/p/438200185?spm=wolai.workspace.0.0.1464c92fIfF20U")

[ GitHub - ryouaki/lsp-uuid: A powerful, fast, small uuid generator for both Browser and Nodejs. Keep sequence and can be deserialized. A powerful, fast, small uuid generator for both Browser and Nodejs. Keep sequence and can be deserialized. - GitHub - ryouaki/lsp-uuid: A powerful, fast, small uuid generator for both Browser and N... https://github.com/ryouaki/lsp-uuid](https://github.com/ryouaki/lsp-uuid " GitHub - ryouaki/lsp-uuid: A powerful, fast, small uuid generator for both Browser and Nodejs. Keep sequence and can be deserialized. A powerful, fast, small uuid generator for both Browser and Nodejs. Keep sequence and can be deserialized. - GitHub - ryouaki/lsp-uuid: A powerful, fast, small uuid generator for both Browser and N... https://github.com/ryouaki/lsp-uuid")

### 随机颜色生成

当你需要获取一个随机颜色

```javascript 
const getRandomColor = () => `#${Math.floor(Math.random() * 0xffffff).toString(16)}`
getRandomColor() // '#4c2fd7'

```


### 颜色格式转换

当你需要将16进制的颜色转换成rgb

```javascript 
const hexToRgb = hex => hex.replace(/^#?([a-f\d])([a-f\d])([a-f\d])$/i, (_, r, g, b) => `#${r}${r}${g}${g}${b}${b}`).substring(1).match(/.{2}/g).map((x) => parseInt(x, 16));
hexToRgb('#00ffff'); // [0, 255, 255]
hexToRgb('#0ff'); // [0, 255, 255]

```
