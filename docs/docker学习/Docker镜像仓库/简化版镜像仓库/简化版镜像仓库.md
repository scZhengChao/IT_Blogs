# 简化版镜像仓库

## 目录

- [安装简化版本Docker镜像仓库](#安装简化版本Docker镜像仓库)
  - [执行上述命令安装Docker镜像仓库](#执行上述命令安装Docker镜像仓库)
  - [查看镜像是否安装成功](#查看镜像是否安装成功)
  - [配置Docker信任地址](#配置Docker信任地址)

Docker官方的Docker Registry是一个**基础版本的Docker镜像仓库**，具备仓库管理的完整功能。

搭建方式比较简单，命令如下：

```markdown 
# docker run 创建并运行镜像仓库
# -d 表示后台
# \ 表示放在一行太长不好看，表示换行
docker run -d \
    #  --restart=always每次都是开机自动启动
    --restart=always \
    # --name registry 表示容器名
    --name registry    \
    # 表示端口号是5000
    -p 5000:5000 \
    # 数据卷
    -v registry-data:/var/lib/registry \
    # 表示镜像
    registry

```


```docker 
docker run -d \
    --restart=always \
    --name registry    \
    -p 5000:5000 \
    -v registry-data:/var/lib/registry \
    registry

```


说明:

> 1.Docker Registry 默认端口号是5000

命令中挂载了一个数据卷registry-data到容器内的/var/lib/registry 目录，这是私有镜像库存放数据的目录。

访问[http://YourIp:5000/v2/\_catalog](http://YourIp:5000/v2/_catalog "http://YourIp:5000/v2/_catalog")可以查看当前私有镜像服务中包含的镜像

## 安装简化版本Docker镜像仓库

### 执行上述命令安装Docker镜像仓库

![](image_xwZB7mIvXq.png)

### 查看镜像是否安装成功

![](image_u9YeK2-Zsz.png)

![](image_wjZq30EHgD.png)

### 配置Docker信任地址

我们的私服采用的是http协议，默认不被Docker信任，所以需要做一个配置：

```markdown 
# 打开要修改的文件
vim /etc/docker/daemon.json
# 添加内容：换成你的linux的ip
"insecure-registries":["http://192.168.200.128:5000"]

# 重加载
systemctl daemon-reload
# 重启docker
systemctl restart docker

```


【1】打开要修改的文件

```markdown 
vim /etc/docker/daemon.json

```


【2】添加内容：

```yaml 
"insecure-registries":["http://192.168.200.128:5000"]

```


![](image_MG5xyYwCFb.png)

【3】重加载

```bash 
systemctl daemon-reload
```


![](image_8oNehaiSvn.png)

【4】重启docker

```bash 
systemctl restart docker

```


![](image_qJ6u5o3Qi8.png)

【5】浏览器直接访问,可以查看当前私有镜像服务中包含的镜像

```markdown 

```


![](image_JScMmjAF2Q.png)
