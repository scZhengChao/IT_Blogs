# 什么是数据卷

数据卷（volume）是**一个虚拟目录**，指向**宿主机文件系统中的某个目录**。

![](image_E_Gk5LKlbT.png)

一旦完成数据卷挂载，对**容器的一切操作都会作用在数据卷对应的宿主机目录了。**

这样，我们操作宿主机的/var/lib/docker/volumes/html目录，就等于操作容器内的/usr/share/nginx/html目录了
