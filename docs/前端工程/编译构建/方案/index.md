# 方案

[环境变量](IT/前端工程/编译构建/方案/环境变量/环境变量.md "环境变量")

[ignore typescript errors](<./ignore typescript errors/index.md> "ignore typescript errors")

[对于element 或者 vue 这种 不会变的js文件；每次打包后的hash值能不能固定不变呢](<./对于element 或者 vue 这种 不会变的js文件；每/对于element 或者 vue 这种 不会变的js文件；每次打包后的hash值能不能固定不变呢.md> "对于element 或者 vue 这种 不会变的js文件；每次打包后的hash值能不能固定不变呢")
