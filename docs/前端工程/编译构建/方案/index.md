# 方案

[环境变量](./环境变量/index.md "环境变量")

[ignore typescript errors](<./ignore typescript errors/index.md> "ignore typescript errors")

[对于element 或者 vue 这种 不会变的js文件；每次打包后的hash值能不能固定不变呢](<./依赖包哈希稳定性/index.md> "对于element 或者 vue 这种 不会变的js文件；每次打包后的hash值能不能固定不变呢")
