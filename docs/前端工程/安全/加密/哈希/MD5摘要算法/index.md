# MD5摘要算法

## 目录

- [安装CryptoJS](#安装CryptoJS)
- [MD5](#MD5)
- [处理json字符串 换行和空格的问题](#处理json字符串-换行和空格的问题)
- [md5的缺陷](#md5的缺陷)
  - [注意事项](#注意事项)

[ md5哈希加密 - MD5在线加密工具 一款可以在线md5加密工具，有16位、32位的字符可选 https://www.bchrt.com/tools/md5jiami/](https://www.bchrt.com/tools/md5jiami/ " md5哈希加密 - MD5在线加密工具 一款可以在线md5加密工具，有16位、32位的字符可选 https://www.bchrt.com/tools/md5jiami/")

# 安装CryptoJS

```typescript 
npm install crypto-js

import CryptoJS from 'crypto-js/crypto-js'

```


# MD5

MD5是一种常用的哈希算法，**用于给任意数据一个“签名”**，下面举例展示了对数字1进行的MD5加密，并打印出加密后的密文。

```typescript 
function Md5 (string) {
  return CryptoJS.MD5(string).toString()
}
// 487f7b22f68312d2c1bbc93b1aea445b


```


# 处理json字符串 换行和空格的问题

```javascript 
import CryptoJS from 'crypto-js';
export function md5Encryption(data: Object) {
  const str = JSON.stringify(data, null, 2).replace(/(\n|\r)/g, '\r\n');
  return CryptoJS.MD5(str).toString().toUpperCase();
}
```


**MD5 并不是一种加密算法，** ​**而是一种摘要算法**，我们也可以叫它哈希函数，**哈希函数可以将无限键值空间中的所有键都均匀地映射到一个指定大小的键值空间中**；

一个好的摘要算法**能够帮助我们保证文件的完整性，避免攻击者的恶意篡改，** 但是加密算法或者加密的功能是 —— 通过某种特定的方式来编码消息或者信息，只有授权方可以访问原始数据，而没有被授权的人无法从密文中获取原文。

由于加密需要同时保证消息的秘密性和完整性，所以加密的过程使用一系列的算法，**MD5 确实可以在加密的过程中作为哈希函数使用来保证消息的完整性**，但是我们还需要另一个算法来保证消息的秘密性，所以由于 MD5 哈希的信息无法被还原，只依靠 MD5 是无法完成加密的。

# md5的缺陷

[ 为什么说用 MD5 存储密码非常危险，这些你该清楚-腾讯云开发者社区-腾讯云 很多软件工程师都认为 MD5 是一种加密算法，然而这种观点其实是大错特错并且十分危险的，作为一个 1992 年第一次被公开的算法，到今天为止已经被发现了一些致命的漏洞，我们在生产环境的任何场景都不应该继续使用 MD5 算法，无论是对数据或者文件的内容进行校验还是用于所谓的『加密』。 https://cloud.tencent.com/developer/article/1805749](https://cloud.tencent.com/developer/article/1805749 " 为什么说用 MD5 存储密码非常危险，这些你该清楚-腾讯云开发者社区-腾讯云 很多软件工程师都认为 MD5 是一种加密算法，然而这种观点其实是大错特错并且十分危险的，作为一个 1992 年第一次被公开的算法，到今天为止已经被发现了一些致命的漏洞，我们在生产环境的任何场景都不应该继续使用 MD5 算法，无论是对数据或者文件的内容进行校验还是用于所谓的『加密』。 https://cloud.tencent.com/developer/article/1805749")

## 注意事项

1. **安全性考虑**：
   - MD5不适用于**安全敏感场景**(已被证明可人为制造碰撞)
   - 安全场景应使用`SHA-256`等更安全的哈希算法
2. **大文件优化**：
   - 采用**流式处理**避免内存问题
   - **分块计算**提高大文件处理效率
3. **局限性**：
   - 不同文件可能产生**相同MD5(但概率极低)**
   - 对刻意构造的碰撞攻击防护不足
4. **性能对比**：
   | 算法      | 输出长度 | 安全性        | 速度         |
   | ------- | ---- | ---------- | ---------- |
   | MD5     | 128位 | 低  \&#x20; | 快  \&#x20; |
   | SHA-1   | 160位 | 中  \&#x20; | 中  \&#x20; |
   | SHA-256 | 256位 | 高          | 慢  \&#x20; |

对于大多数非安全敏感的文件比对场景，MD5仍然是简单高效的选择。但在需要高度安全性的场合，建议使用SHA-256等更安全的算法。
