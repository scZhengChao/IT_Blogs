# RDB

## 目录

- [2、RDB持久化介绍\[了解\]](#2RDB持久化介绍了解)

### 2、RDB持久化介绍\[了解]

RDB是默认的持久化方式。这种方式就是将内存中数据以快照的方式写入到二进制文件中，默认的文件名为：**dump.rdb。**

补充：快照：当前内存中数据的状态。

![](./assets/image/image_7nuyMeV4Ar.png)

可以**通过配置设置自动做快照持久化的方式。** 如下面配置的是RDB方式数据持久化时机，必须两个条件都满足的情况下才进行持久化的操作：

| **关键字**​  | **时间(秒)** ​ | **修改键数**​  | **解释**​               |
| --------- | ----------- | ---------- | --------------------- |
| **save**​ | **900**​    | **1**​     | 到了15分钟修改了1个键，则发起快照保存  |
| **save**​ | **300**​    | **10**​    | 到了5分钟修改了10个键，则发起快照保存  |
| **save**​ | **60**​     | **10000**​ | 到了1分钟，修改了1万个键，则发起快照保存 |

![](./assets/image/image_1SoVZ4KQr0.png)

```markdown 
缺点:
         可能导致数据丢失。因为RDB是每隔一段时间写入数据，所以系统一旦在定时持久化之前出现宕机现象，此前没有来得及写入磁盘的数据都将丢失。
 优点:
         持久化效率高,持久化的是内存中的数据
         数据库宕机后，数据恢复的效率要更高；

```
