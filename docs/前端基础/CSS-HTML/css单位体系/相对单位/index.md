# 相对单位

相对单位就是相对于另一个长度的长度。CSS中的相对单位主要分为两大类：

- 字体相对单位，他们都是根据font-size来进行计算的。常见的字体相对单位有：em、rem、ex、ch。
- 视窗相对单位，他们都是根据视窗大小来决定的。常见的视窗相对单位有vw、vh、vmax、vmin。

下面就来看看这些常见的CSS单位。

[cqw/cqh](./cqw-cqh/index.md "cqw/cqh")

[em 和 rem](<./em 和 rem/index.md> "em 和 rem")

[ex 和 ch](<./ex 和 ch/index.md> "ex 和 ch")

[vw、vh、vmax 和 vmin](<./vw、vh、vmax 和 vmin/index.md> "vw、vh、vmax 和 vmin")
