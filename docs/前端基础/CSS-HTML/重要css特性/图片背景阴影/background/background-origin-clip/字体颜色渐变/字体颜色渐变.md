# 字体颜色渐变

## 目录

- [background-clip](#background-clip)
- [mask](#mask)

![](image_z_l9n4sVqh.png)

### background-clip

CSS3设置文字渐变色，这里主要使用到的CSS属性有-webkit-background-clip,linear-gradient等

原理:

这里主要使用的是背景的裁剪技术，即

```html 
-webkit-background-clip: text;

```


**即有文字的地方才有背景，其他地方不被背景覆盖。**

然后**将文字的颜色设置为透明，这样背景颜色就可以透过透明显示出文字，从而实现文字的渐变色。** 所以，我们只需要设置文字的包含快或者包含行元素的背景渐变色即可

```html 
<style>
.box{
    background: linear-gradient(to left ,#ffccff,#ccffff, #9966ff);
    text-align: center;
    font-size: 30px;
    -webkit-background-clip: text;
    color:transparent;
}
</style>
        <span class="box">
        TESTCONTEXT<br>
        HELLOWORLD
    </span>

```


当然，我们也可以设置背景图片来实现渐变

```html 
background-image: url("1.jpg");
```


`background-clip：text` 目前支持大多浏览器，但是 IE 不可以

![](image_WF-DA8Nw2b.png)

### mask

```html 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <style>
        .text-gradient {
            position: relative;
            color: blue;
            font-size: 30px;
        }
        .text-gradient:before {
            content: attr(text);
            position: absolute;
            z-index: 10;
            color: orange;
            -webkit-mask: linear-gradient(to right, transparent, orange);
        }
    </style>
</head>
<body>
    <div text="文字渐变" class="text-gradient">文字渐变</div>
</body>
</html>

```


实现原理：

- .text-gradient:before 用来生成一个新元素，content:attr(text) 让新元素的内容与原文本的内容相同，color:orange 将新元素文本设置为橙色
- -webkit-mask: linear-gradient(to right, transparent, orange) 为新元素添加了一个从左到右，从透明到橙色的渐变遮罩，before 元素中与mask的 transparent 的重叠部分也变成了透明
- before 新元素与原 div 蓝色文本叠加，形成了从左至右从蓝色到橙色的渐变效果

**注意：**

- `mask` 目前支持大多浏览器，但是 IE 不可以 &#x20;

  ![适配](https://img-blog.csdnimg.cn/7ab991c189fc4098a5c40aba6ecdcdeb.png "适配")
