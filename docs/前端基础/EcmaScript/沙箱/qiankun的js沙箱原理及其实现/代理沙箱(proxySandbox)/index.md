# 代理沙箱(proxySandbox)

`qiankun`基于`es6`的`Proxy`实现了两种应用场景不同的沙箱，一种是`legacySandbox`(单例)，一种是`proxySandbox`(多例)。因为都是基于Proxy实现的，所以都称为代理沙箱。

[legacySandbox(单例沙箱)](./legacySandbox(单例沙箱)/index.md "legacySandbox(单例沙箱)")

[proxySandbox(多例沙箱)](./proxySandbox(多例沙箱)/index.md "proxySandbox(多例沙箱)")
