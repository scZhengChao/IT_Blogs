# 手势响应系统 和 动画

## 目录

- [手势响应](#手势响应)
  - [参考来源：](#参考来源)
  - [一.申请成为事件响应者](#一申请成为事件响应者)
  - [二.事件响应者](#二事件响应者)
  - [三：事件数据结构](#三事件数据结构)
  - [四：嵌套 组件事件处理](#四嵌套-组件事件处理)
  - [五：手势识别](#五手势识别)
    - [响应者的生命周期](#响应者的生命周期)
    - [捕获 ShouldSet 事件处理](#捕获-ShouldSet-事件处理)
  - [六.PanResponder](#六PanResponder)
    - [原生事件](#原生事件)
    - [gestureState对象](#gestureState对象)
  - [七：总结注意](#七总结注意)
  - [手势库  react-native-gesture-handler](#手势库-react-native-gesture-handler)
    - [为什么要使用这个库 ](#为什么要使用这个库-)
    - [组件与方法说明 ](#组件与方法说明-)
    - [与PanResponder对照 ](#与PanResponder对照-)
    - [更详细的api：](#更详细的api)
- [八：动画Animate](#八动画Animate)
  - [概览](#概览)
  - [配置动画](#配置动画)
  - [API:](#API)
    - [event ](#event-)
    - [spring](#spring)
    - [Animated.parallel()](#Animatedparallel)
- [九：Animate.Value()/ValueXY()](#九AnimateValueValueXY)
  - [API](#API)
    - [1.setValue()](#1setValue)
    - [2.flattenOffset()](#2flattenOffset)
    - [3.setoffset()](#3setoffset)
    - [4.注释：](#4注释)
    - [5.interpolate](#5interpolate)
- [10：案例文档](#10案例文档)
  - [基本api：](#基本api)
  - [案例一：拖拽加吸边](#案例一拖拽加吸边)
  - [案例一扩展](#案例一扩展)
  - [案例二 ：滚动导致颜色渐变](#案例二-滚动导致颜色渐变)
  - [进阶api](#进阶api)
  - [手势冲突](#手势冲突)
    - [拖拽和touch 的冲突](#拖拽和touch-的冲突)
  - [翻转动画：](#翻转动画)

# 手势响应

## 参考来源：

       以官网为准

[手势响应系统 · React Native 中文网 移动设备上的手势识别要比在 web 上复杂得多。用户的一次触摸操作的真实意图是什么，App 要经过好几个阶段才能判断。比如 App 需要判断用户的触摸到底是在滚动页面，还是滑动一个 widget，或者只是一个单纯的点击。甚至随着持续时间的不同，这些操作还会转化。此外，还有多点同时触控的情况。 https://reactnative.cn/docs/gesture-responder-system](https://reactnative.cn/docs/gesture-responder-system "手势响应系统 · React Native 中文网 移动设备上的手势识别要比在 web 上复杂得多。用户的一次触摸操作的真实意图是什么，App 要经过好几个阶段才能判断。比如 App 需要判断用户的触摸到底是在滚动页面，还是滑动一个 widget，或者只是一个单纯的点击。甚至随着持续时间的不同，这些操作还会转化。此外，还有多点同时触控的情况。 https://reactnative.cn/docs/gesture-responder-system")

[React Native手势处理详解\_iosJohnson的博客-CSDN博客 转自：http://www.race604.com/react-native-touch-event/，深度好文！！！触控是移动设备的核心功能，也移动应用交互的基础，Android 和 iOS 各自都有完善的触摸事件处理机制。React Native（以下简称 RN）提供了一套统一的处理方式，能够方便的处理界面中组件的触摸事件、用户手势等。本文尝试介绍 RN 中触摸事件处理。1. RN https://blog.csdn.net/iosjohnson/article/details/53127830](https://blog.csdn.net/iosjohnson/article/details/53127830 "React Native手势处理详解_iosJohnson的博客-CSDN博客 转自：http://www.race604.com/react-native-touch-event/，深度好文！！！触控是移动设备的核心功能，也移动应用交互的基础，Android 和 iOS 各自都有完善的触摸事件处理机制。React Native（以下简称 RN）提供了一套统一的处理方式，能够方便的处理界面中组件的触摸事件、用户手势等。本文尝试介绍 RN 中触摸事件处理。1. RN https://blog.csdn.net/iosjohnson/article/details/53127830")

[PanResponder · React Native 中文网 PanResponder类可以将多点触摸操作协调成一个手势。它使得一个单点触摸可以接受更多的触摸操作，也可以用于识别简单的多点触摸手势。 https://reactnative.cn/docs/panresponder](https://reactnative.cn/docs/panresponder "PanResponder · React Native 中文网 PanResponder类可以将多点触摸操作协调成一个手势。它使得一个单点触摸可以接受更多的触摸操作，也可以用于识别简单的多点触摸手势。 https://reactnative.cn/docs/panresponder")

[“指尖上的魔法” -- 谈谈React-Native中的手势 · Issue #1 · jabez128/jabez128.github.io React-Native是一款由Facebook开发并开源的框架，主要卖点是使用JavaScript编写原生的移动应用。从2015年3月份开源到现在，已经差不多有半年。目前，React-Native正在以几乎每周一个版本的速度进行快速迭代，开源社区非常活跃。2015年9月15日，React-Native正式宣布支持安卓，并在项目主页中更新了相关文档，这意味着React-Native已经完全覆... https://github.com/jabez128/jabez128.github.io/issues/1](https://github.com/jabez128/jabez128.github.io/issues/1 "“指尖上的魔法” -- 谈谈React-Native中的手势 · Issue #1 · jabez128/jabez128.github.io React-Native是一款由Facebook开发并开源的框架，主要卖点是使用JavaScript编写原生的移动应用。从2015年3月份开源到现在，已经差不多有半年。目前，React-Native正在以几乎每周一个版本的速度进行快速迭代，开源社区非常活跃。2015年9月15日，React-Native正式宣布支持安卓，并在项目主页中更新了相关文档，这意味着React-Native已经完全覆... https://github.com/jabez128/jabez128.github.io/issues/1")

需求：android 可以监控和修改物理返回，但是ios没有想到方法，为了定制ios的手势返回，这是背景

## 一.申请成为事件响应者

          默认情况下，触摸事件输入不会直接传递给组件，不能进行事件响应处理，也就是非事件响应者。如果组件要进行触摸事件处理，首先要

**申请成为事件响应者**

，组件有如下两个属性可以做这样的申请：

- View\.props.onStartShouldSetResponder，这个属性接收一个回调函数，函数原型是 function(evt): bool，在触摸事件开始（touchDown）的时候，RN 会回调此函数，询问组件是否需要成为事件响应者，接收事件处理，如果返回 true，表示需要成为响应者；
- View\.props.onMoveShouldSetResponder，它和

             前一个属性类似，不过这是触摸是进行过程中（touchMove），RN 询问组件是否要成为响应者，返回 true 表示是。假如组件通过上面的方法返回了 true，表示发出了申请要成为事件响应者请求，想要接收后续的事件输入。

**因为同一时刻，只能有一个事件处理响应者，RN 还需要协调所有组件的事件处理请求，所以不是每个组件申请都能成功**

，RN 通过如下两个回调来通知告诉组件它的申请结果，：

- View\.props.onResponderGrant: (evt) => {}：表示申请成功，组件成为了事件处理响应者，这时组件就开始接收后序的触摸事件输入。一般情况下，这时开始，组件进入了激活状态，并进行一些事件处理或者手势识别的初始化。
- View\.props.onResponderReject: (evt) => {}：表示申请失败了，这意味者其他组件正在进行事件处理，并且它不想放弃事件处理，所以你的申请被拒绝了，后续输入事件不会传递给本组件进行处理。

## 二.事件响应者

如果通过上面的步骤，组件申请成为了事件响应者，后续的事件输入都会通过回调函数通知到组件，如下：

- View\.props.onResponderStart: (evt) => {}：表示手指按下时，成功申请为事件响应者的回调；
- View\.props.onResponderMove: (evt) => {}：表示触摸手指移动的事件，这个回调可能非常频繁，所以这个回调函数的内容需要尽量简单；
- View\.props.onResponderRelease: (evt) => {}：表示触摸完成（touchUp）的时候的回调，表示用户完成了本次的触摸交互，这里应该完成手势识别的处理，这以后，组件不再是事件响应者，组件取消激活。
- View\.props.onResponderEnd: (evt) => {}：表示组件结束事件响应的回调。

从前面的图中也看到，在组件成为事件响应者期间，其他组件也可能会申请触摸事件处理。此时 RN 会通过回调询问你是否可以释放响应者角色让给其他组件。回调如下：

```javascript 
 View.props.onResponderTerminationRequest: (evt) => bool   
```


如果回调函数返回为 true，则表示同意释放响应者角色，同时会回调如下函数，通知组件事件响应处理被终止了：

```javascript 
 View.props.onResponderTerminate: (evt) => {}   
```


这个回调也会发生在系统直接终止组件的事件处理，例如用户在触摸操作过程中，突然来电话的情况。

## 三：事件数据结构

        从前面我们看到，触摸事件处理的回调都有一个 evt 参数，包含一个触摸事件数据 nativeEvent。nativeEvent 的详细内容如下：

- identifier：触摸的 ID，一般对应手指，在多点触控的时候，用来区分是哪个手指的触摸事件；
- locationX 和 locationY：触摸点相对组件的位置；
- pageX 和 pageY：触摸点相对于屏幕的位置；
- timestamp：当前触摸的事件的时间戳，可以用来进行滑动计算；
- target：接收当前触摸事件的组件 ID；
- changedTouches：evt 数组，从上次回调上报的触摸事件，到这次上报之间的所有事件数组。因为用户触摸过程中，会产生大量事件，有时候可能没有及时上报，系统用这种方式批量上报；
- touches：evt 数组，多点触摸的时候，包含当前所有触摸点的事件。

         这些数据中，最常用的是 locationX 和 locationY 数据，需要注意的是，因为这里是 Native 的数据，所以他们的单位是实际像素。如果要转换为 RN 中的逻辑单位，可以示使用如下方法：

```javascript 
 var pX = evt.nativeEvent.locationX / PixelRatio.get();  
```


## 四：嵌套 组件事件处理

&#x20;         上一小节介绍的都是针对单个组件来说，事件处理的流程和机制。但是前面也提到了，当组件需要作为事件处理响应者时，需要通过onStartShouldSetResponder或者 onMoveShouldSetResponder回调返回值为 true来申请。

&#x20;      假如当多个件嵌套的时候，这两个回调都返回了 true的时候，但是同一个只能有一个事件处理响应者，这种情况怎么处理呢？为了便于描述，假设我们的组件布局如下：

         在 RN 中，默认情况下使用冒泡机制，响应最深的组件最先开始响应，所以前面描述的这种情况，如图中，如果 A、B、C 三个组件的 on\*ShouldSetResponder 都返回为 true，那么只有 C 组件会得到响应成为响应者。这种机制才能保证了界面所有的组件才能得到响应。但是有些情况下，可能父组件可能需要处理事件，而禁止子组件响应。RN 提供了一个劫持机制，也就是在触摸事件往下传递的时候，先询问父组件是否需要劫持，不给子组件传递事件，也就是如下两个回调：

- View\.props.onStartShouldSetResponderCapture：这个属性接收一个回调函数，函数原型是 function(evt): bool，在触摸事件开始（touchDown）的时候，RN 容器组件会回调此函数，询问组件是否要劫持事件响应者设置，自己接收事件处理，如果返回 true，表示需要劫持；
- View\.props.onMoveShouldSetResponderCapture：此函数类似，不过是在触摸移动事件（touchMove）询问容器组件是否劫持。

## 五：手势识别

       前面只是介绍了简单的触摸事件处理机制及其使用方法，其实连续的触摸事件，可以组成一些更高级手势，例如我们最常见的滑动屏幕内容，双指缩放（Pinch）或者旋转图片都是通过手势识别完成的。

         因为有些手势是很常用的，RN 也提供了内置的手势识别库 PanResponder，它封装了上面的事件回调函数，对触摸事件数据进行加工，完成滑动手势识别，向我们提供更加高级有意义的接口，如下：

### 响应者的生命周期

一个 View 只要实现了正确的协商方法，就可以成为触摸事件的响应者。我们通过两个方法去“询问”一个 View 是否愿意成为响应者：

- View\.props.onStartShouldSetResponder: (evt) => true, - 在用户开始触摸的时候（手指刚刚接触屏幕的瞬间），是否愿意成为响应者？
- View\.props.onMoveShouldSetResponder: (evt) => true, - 如果 View 不是响应者，那么在每一个触摸点开始移动（没有停下也没有离开屏幕）时再询问一次：是否愿意响应触摸交互呢？

**如果 View 返回 true，并开始尝试成为响应者，那么会触发下列事件之一:**

- View\.props.onResponderGrant: (evt) => {} - View 现在要开始响应触摸事件了。这也是需要做高亮的时候，使用户知道他到底点到了哪里。
- View\.props.onResponderReject: (evt) => {} - 响应者现在“另有其人”而且暂时不会“放权”，请另作安排。

**如果 View 已经开始响应触摸事件了，那么下列这些处理函数会被一一调用：**

- View\.props.onResponderMove: (evt) => {} - 用户正在屏幕上移动手指时（没有停下也没有离开屏幕）。
- View\.props.onResponderRelease: (evt) => {} - 触摸操作结束时触发，比如"touchUp"（手指抬起离开屏幕）。
- View\.props.onResponderTerminationRequest: (evt) => true - 有其他组件请求接替响应者，当前的 View 是否“放权”？返回 true 的话则释放响应者权力。
- View\.props.onResponderTerminate: (evt) => {} - 响应者权力已经交出。这可能是由于其他 View 通过onResponderTerminationRequest请求的，也可能是由操作系统强制夺权（比如 iOS 上的控制中心或是通知中心）。

### 捕获 ShouldSet 事件处理

onStartShouldSetResponder与onMoveShouldSetResponder**是以冒泡的形式调用的，**即**嵌套最深的节点最先调用。**

这意味着当多个 View 同时在\*ShouldSetResponder中返回 true 时，**最底层的 View 将优先“夺权”**。在多数情况下这并没有什么问题，因为这样可以确保所有控件和按钮是可用的。

但是有些时候，**某个父 View 会希望能先成为响应者。我们可以利用“捕获期”来解决这一需求**。响应系统在从最底层的组件**开始冒泡之前，会首先执行一个“捕获期**”，在此期间会触发on\*ShouldSetResponderCapture系列事件。因此，**如果某个父 View 想要在触摸操作开始时阻止子组件成为响应者，那就应该处理onStartShouldSetResponderCapture事件并返回 true 值。**

- View\.props.onStartShouldSetResponderCapture: (evt) => true,
- View\.props.onMoveShouldSetResponderCapture: (evt) => true,
- onMoveShouldSetPanResponder: (e, gestureState) => bool  //如果 View 不是响应者，那么在每一个触摸点开始移动（没有停下也没有离开屏幕）时再询问一次：是否愿意响应触摸交互呢？
- onMoveShouldSetPanResponderCapture: (e, gestureState) => bool  *// 拦截子组件的移动手势传递,是否拦截*
- onStartShouldSetPanResponder: (e, gestureState) => bool // 单机手势是否可以成为响应者
- onStartShouldSetPanResponderCapture: (e, gestureState) => bool   // 拦截子组件的单击手势传递,是否拦截
- onPanResponderReject: (e, gestureState) => {...}  *// 手势申请失败,未成为响应者的回调，响应者现在“另有其人”而且暂时不会“放权”*
- onPanResponderGrant: (e, gestureState) => {...}   // 单击手势监听回调
- onPanResponderStart: (e, gestureState) => {...}
- onPanResponderEnd: (e, gestureState) => {...}  *// 手势动作结束回调*
- onPanResponderRelease: (e, gestureState) => {...}  // 手势释放, 响应者释放回调
- onPanResponderMove: (e, gestureState) => {...}  *// 移动手势监听回调*
- onPanResponderTerminate: (e, gestureState) => {...}  *// 当前手势被强制取消的回调*
- onPanResponderTerminationRequest: (e, gestureState) => {...}有其他组件请求接替响应者，当前的是否“放权”？返回 true 的话则释放响应者权力。
- onShouldBlockNativeResponder: (e, gestureState) => bool   // 返回一个布尔值，决定当前组件是否应该阻止原生组件成为JS响应者

        可以看到，这些接口与前面接收的基础回调基本上是一一对应的，其功能也是类似，这里就不再赘述。这里有一个特别的回调 onShouldBlockNativeResponder 表示是否用 Native 平台的事件处理，默认是禁用的，全部使用 JS 中的事件处理，注意此函数目前只能在 Android 平台上使用。不过这里回调函数都有一个新的

**参数 gestureState，这是与滑动相关的数据，是对基本触摸数据的分析处理，它的内容如下：**

- stateID：滑动手势的 ID，在一次完整的交互中此 ID 保持不变；
- moveX 和 moveY：自上次回调，手势移动距离；
- x0 和 y0：滑动手势识别开始的时候的在屏幕中的坐标；
- dx 和 dy：从手势开始时，到当前回调是移动距离；
- vx 和 vy：当前手势移动的速度；
- numberActiveTouches：当期触摸手指数量。

## 六.PanResponder

         PanResponder类可以将多点触摸操作协调成一个手势。它使得一个单点触摸可以接受更多的触摸操作，也可以用于识别简单的多点触摸手势。

         默认情况下PanResponder会通过InteractionManager来阻止长时间运行的 JS 事件打断当前的手势活动。

        它提供了一个对[触摸响应系统](https://reactnative.cn/docs/gesture-responder-system "触摸响应系统")

&#x20;   响应器的可预测的包装。对于每一个处理函数，它在原生事件之外提供了一个新的gestureState对象：

```javascript 
 onPanResponderMove: (event, gestureState) => {}
```


### 原生事件

是指由以下字段组成的合成触摸事件：

- nativeEvent
  - changedTouches - 在上一次事件之后，所有发生变化的触摸事件的数组集合（即上一次事件后，所有移动过的触摸点）
  - identifier - 触摸点的 ID
  - locationX - 触摸点相对于父元素的横坐标
  - locationY - 触摸点相对于父元素的纵坐标
  - pageX - 触摸点相对于根元素的横坐标
  - pageY - 触摸点相对于根元素的纵坐标
  - target - 触摸点所在的元素 ID
  - timestamp - 触摸事件的时间戳，可用于移动速度的计算
  - touches - 当前屏幕上的所有触摸点的集合

### gestureState对象

有如下的字段：

- stateID - 触摸状态的 ID。在屏幕上有至少一个触摸点的情况下，这个 ID 会一直有效。
- moveX - 最近一次移动时的屏幕横坐标
- moveY - 最近一次移动时的屏幕纵坐标
- x0 - 当响应器产生时的屏幕坐标  --- 触点相对屏幕的坐标
- y0 - 当响应器产生时的屏幕坐标   --- 触点相对屏幕的纵坐标
- dx - 从触摸操作开始时的累计横向路程
- dy - 从触摸操作开始时的累计纵向路程
- vx - 当前的横向移动速度
- vy - 当前的纵向移动速度
- numberActiveTouches - 当前在屏幕上的有效触摸点的数量

dy+y0 = moveY movey 是移动后的 屏幕纵坐标 dy是你点上去的坐标，dy 是你移动的坐标

locationx是触摸点相当于父元素的坐标， pagey 是触摸点想当与根元素的坐标

```javascript 
 componentWillMount: function() {
    this._panResponder = PanResponder.create({
      // 要求成为响应者：
      onStartShouldSetPanResponder: (evt, gestureState) => true,
      onStartShouldSetPanResponderCapture: (evt, gestureState) => true,
      onMoveShouldSetPanResponder: (evt, gestureState) => true,
      onMoveShouldSetPanResponderCapture: (evt, gestureState) => true,

      onPanResponderGrant: (evt, gestureState) => {
        // 开始手势操作。给用户一些视觉反馈，让他们知道发生了什么事情！

        // gestureState.{x,y} 现在会被设置为0
      },
      onPanResponderMove: (evt, gestureState) => {
        // 最近一次的移动距离为gestureState.move{X,Y}

        // 从成为响应者开始时的累计手势移动距离为gestureState.d{x,y}
      },
      onPanResponderTerminationRequest: (evt, gestureState) => true,
      onPanResponderRelease: (evt, gestureState) => {
        // 用户放开了所有的触摸点，且此时视图已经成为了响应者。
        // 一般来说这意味着一个手势操作已经成功完成。
      },
      onPanResponderTerminate: (evt, gestureState) => {
        // 另一个组件已经成为了新的响应者，所以当前手势将被取消。
      },
      onShouldBlockNativeResponder: (evt, gestureState) => {
        // 返回一个布尔值，决定当前组件是否应该阻止原生组件成为JS响应者
        // 默认返回true。目前暂时只支持android。
        return true;
      },
    });
  },

  render: function() {
    return (
      <View {...this._panResponder.panHandlers} />
    );
  },
```


## 七：总结注意

         另外需要注意，因为 RN 的异步通信和执行机制，前面描述的所有回调函数都是在 JS 线程中，并不是 Native 的 UI 线程，而 Native 平台的 Touch 事件都是在 UI 线程中。

**所以在 JS 中通过 Touch 或者手势实现动画，可能会延迟的问题**。

## 手势库  react-native-gesture-handler

react-native-gesture-handler

### 为什么要使用这个库&#x20;

**更加的流畅、可靠。**

react-native自带的PanResponder手势监视器由JS响应器系统控制而react-native-gesture-handler是在UI线程中识别和跟踪手势。&#x20;

若使用RN官方提供的手势管理在与发生在主线程上的触摸交互(如iOS滑块或任何滚动视图)时，经常会遇到问题。==由于主线程必须同步决定JS或滚动视图是否应该成为响应器，而JS只能异步响应并不能立即拒绝Native事件的响应，导致手势操作被这些Native组件劫持。==&#x20;

*ps: 上面提到的JS不能立即拒绝Native事件的响应对应PanResponder中的onPanResponderTerminationRequest方法，当该方法返回false时表示拒绝其他组件响应当前的手势。*

问题场景示例：&#x20;

1. 使用react-native-scroll-tab-view组件作为导航，左右切换视图。
2. 在react-native-scroll-tab-view内嵌套了Slider组件或者其他使用PanResponder手势监视器实现了某些手势操作。
3. 使用时常常会出现在这些子组件做滑动操作时，触发了react-native-scroll-tab-view组件的滑动的问题。

详情react-native项目中的这个issue：

[How to stopPropagation touch event](https://github.com/facebook/react-native/issues/1046 "How to stopPropagation touch event")

我解决这个问题过程: 

[如何在RN的可滚动组件内设置一个禁止滚动的区域](https://github.com/zgatrying/react-native-practices/issues/25 "如何在RN的可滚动组件内设置一个禁止滚动的区域")

### 组件与方法说明&#x20;

**组件通用属性、方法**

[Common handler properties](https://kmagiera.github.io/react-native-gesture-handler/docs/handler-common.html "Common handler properties")

```vue 
 enabled：是否响应手势操作。
shouldCancelWhenOutside：当前手势离开当前组件区域时是否进入CANCELLED或FAILD状态。
simultaneousHandlers
waitFor：等待其他事件结束时才响应手势操作
hitSlop：可以控制视图区域的哪个部分来开始识别手势。与View组件的hitSlop类似
onGestureEvent：手势ACTIVE状态时执行的回调
onHandlerStateChange：手势状态改变时的回调
```


**事件参数**

onHadlerStateChange与onGestureEvent回调的参数event(主要用到的就是nativeEvent属性)&#x20;

```vue 
 
//event的属性声明
interface event {
    nativeEvent: nativeEvent
    //...
}
 
//nativeEvent的属性声明
interface nativeEvent {
    absoluteX: number //相对于根视图，指针的当前位置的X坐标（当放置多个手指时的手指或前导指针）。
    absoluteY: number //相对于根视图，指针的当前位置的Y坐标（当放置多个手指时的手指或前导指针）。
    handlerTag: number
    numberOfPointers: number //表示当前放置在屏幕上的指针（手指）的数量。
    state: number //手势处理程序的当前状态
    translationX: number //手势开始到目前为止在水平方向上的移动距离。它与PanResponder中的dx类似
    translationY: number //手势开始到目前为止在垂直方向上的移动距离。
    velocityX: number //水平移速
    velocityY: number //垂直移速
    x: number //当前手势位置相对于附加PanGestureHandler的视图的X
    y: number //当前手势位置相对于附加PanGestureHandler的视图的Y
}
```


手势操作过程中，react-native-gesture-handler提供Handlers组件的State会不断变化，开发者根据State来响应不同状态下的操作。详情查看

[Handler State](https://kmagiera.github.io/react-native-gesture-handler/docs/state.html "Handler State")

### 与PanResponder对照&#x20;

（真的良心了一些）

| 说明                           | PanGestureHandler                                 | PanResponder                                              |
| ---------------------------- | ------------------------------------------------- | --------------------------------------------------------- |
| 是否声明成为触摸手势响应者或是否声明成为移动手势的响应者 | -                                                 | onStartShouldSetPanResponder ,onMoveShouldSetPanResponder |
| 触摸手势开始                       | onHadlerStateChange, State.BEGAN                  | onPanResponderGrant                                       |
| 手势移动过程中执行的回调                 | onHadlerStateChange, onGestureEvent, State.ACTIVE | onPanResponderMove                                        |
| 手势释放                         | onHadlerStateChange, State.END或State.FAILED       | onPanResponderRelease                                     |
| 是否将响应手势的操作交给其他组件             | -                                                 | onPanResponderTerminationRequest                          |
| 手势操作被其他组件或事件打断后的回调           | -                                                 | onPanResponderTerminate                                   |

使用对比：&#x20;

PanGestureHandler&#x20;

```vue 
 import {PanGestureHandler, State} from 'react-native-gesture-handler'
 
//...
 
<PanGestureHandler
    onHadlerStateChange = ({nativeEvent}) => {
        switch (nativeEvent.state) {
          case State.UNDETERMINED:
            console.log('等待手势')
            break;
          case State.BEGAN:
            console.log('手势开始')
            break;
          case State.CANCELLED:
            console.log('手势取消')
            break;
          case State.ACTIVE:
            console.log('手势活跃')
            break;
          case State.END:
            console.log('手势结束')
            break;
          case State.FAILED:
            console.log('失败')
            break;
          default:
            console.log('其他')
            break;
        }
    }
    onGestureEvent = ({ nativeEvent }) => {
    
    }
>
    <Animated.View 
        //...
    >
    </Animated.View>
</PanGestureHandler>
```


PanResponder

```vue 
 import {PanResponder} from 'react-native'
 
<Animated.View
    {
        ...PanResponder.create({
            onStartShouldSetPanResponder: this._handleStartShouldSetPanResponder,
            onMoveShouldSetPanResponder: this._handleMoveShouldSetPanResponder,
            onPanResponderGrant: this._handlePanResponderGrant,
            onPanResponderMove: this._handlePanResponderMove,
            onPanResponderRelease: this._handlePanResponderEnd,
            onPanResponderTerminationRequest: this._handlePanResponderRequestEnd,
            onPanResponderTerminate: this._handlePanResponderEnd,
        }).panHandlers
    }
>
</Animated.View>
```


### 更详细的api：

官方文档：

[Hello from React Native Gesture Handler | React Native Gesture Handler Description will go into a meta tag in \<head /> https://docs.swmansion.com/react-native-gesture-handler/index.html](https://docs.swmansion.com/react-native-gesture-handler/index.html "Hello from React Native Gesture Handler | React Native Gesture Handler Description will go into a meta tag in <head /> https://docs.swmansion.com/react-native-gesture-handler/index.html")

# 八：动画Animate

[Animated · React Native 中文网 Animated库旨在使动画变得流畅，强大并易于构建和维护。Animated侧重于输入和输出之间的声明性关系，以及两者之间的可配置变换，此外还提供了简单的 start/stop方法来控制基于时间的动画执行。 https://reactnative.cn/docs/animated](https://reactnative.cn/docs/animated "Animated · React Native 中文网 Animated库旨在使动画变得流畅，强大并易于构建和维护。Animated侧重于输入和输出之间的声明性关系，以及两者之间的可配置变换，此外还提供了简单的 start/stop方法来控制基于时间的动画执行。 https://reactnative.cn/docs/animated")

```javascript 
import React, { Component } from "react"; 
import { Animated, Text, View, StyleSheet, Button } from "react-native";
class App extends Component {   
    // fadeAnim will be used as the value for opacity. Initial Value: 0  
    state = {     
      fadeAnim: new Animated.Value(0)   
    };   
  fadeIn = () => {     
    // Will change fadeAnim value to 1 in 5 seconds     
    Animated.timing(this.state.fadeAnim, {       toValue: 1,       duration: 5000     }).start();  
  };   
  fadeOut = () => {     
    // Will change fadeAnim value to 0 in 5 seconds    
    Animated.timing(this.state.fadeAnim, {       toValue: 0,       duration: 5000     }).start();  
  };   
  render() {    
    return (       
      <View style={styles.container}>        
          <Animated.View          
                style={[ styles.fadingContainer,             
                    {               
                      opacity: this.state.fadeAnim 
                         // Bind opacity to animated value             
                     }           
                ]}>           
            <Text style={styles.fadingText}>Fading View!</Text>        
          </Animated.View>         
          <View style={styles.buttonRow}>           
              <Button title="Fade In" onPress={this.fadeIn} />          
              <Button title="Fade Out" onPress={this.fadeOut} />         
          </View>       
  </View>     
);   
} }
const styles = StyleSheet.create({   
  container: {     
    flex: 1,     
    alignItems: "center",     
    justifyContent: "center"  
  },  
  fadingContainer: {     
    paddingVertical: 8,     
    paddingHorizontal: 16,     
    backgroundColor: "powderblue"  
  },   
  fadingText: {     
    fontSize: 28,     
    textAlign: "center",     
    margin: 10  
  },  
  buttonRow: {     
    flexDirection: "row",    
    marginVertical: 16   
  } 
}); 
export default App;

```


## 概览

Animated提供了两种类型的值：

- [Animated.Value()](https://reactnative.cn/docs/animated#value "Animated.Value()")用于单个值
- [Animated.ValueXY()](https://reactnative.cn/docs/animated#valuexy "Animated.ValueXY()")用于矢量值

Animated.Value可以绑定到样式或是其他属性上，也可以进行插值运算。单个Animated.Value可以用在任意多个属性上。

## 配置动画

Animated提供了三种动画类型。每种动画类型都提供了特定的函数曲线，用于控制动画值从初始值变化到最终值的变化过程：

- [Animated.decay()](https://reactnative.cn/docs/animated#decay "Animated.decay()")以指定的初始速度开始变化，然后变化速度越来越慢直至停下。
- [Animated.spring()](https://reactnative.cn/docs/animated#spring "Animated.spring()")提供了一个基础的弹簧物理模型.
- [Animated.timing()](https://reactnative.cn/docs/animated#timing "Animated.timing()")使用[easing 函数](https://reactnative.cn/docs/easing "easing 函数")让数值随时间动起来。

大多数情况下你应该使用timing()。默认情况下，它使用对称的 easeInOut 曲线，将对象逐渐加速到全速，然后通过逐渐减速停止结束。

## API:

### event&#x20;

![  ](5e25d1223ce17b9d36859eb0b8694a8c_OAiThWA3A7.png "  ")

\*\*event接受一个映射的数组，对应的解开每个值，然后调用所有对应的输出的 \*\*

**etValue**

**方法。例如：**

1. static event(argMapping,config?)

```javascript 
onScroll={Animated.event(

   [{nativeEvent: {contentOffset: {x: this._scrollX}}}],

   {listener: (event) => console.log(event)}, // 可选的异步监听函数
)}
...
onPanResponderMove: Animated.event([
   null,                // 忽略原始事件
   {dx: this._panX}],    // 手势状态参数
  {listener: (event, gestureState) => console.log(event, gestureState)}, // 可选的异步监听函数
),
```


Config 参数有以下这些属性：

- listener: 可选的异步监听函数
- useNativeDriver: 启用原生动画驱动。默认不启用(false)。

**注意： event 最好放在生命周期里，里面映射的this.state会报找不到；这个时候不要把state放在constructor就可以了；原因目前还没找到；肯定是this的指向问题**

### spring

[https://reactnative.cn/docs/animated#springh](https://reactnative.cn/docs/animated#springh "https://reactnative.cn/docs/animated#springh")官网

      根据基于[阻尼谐振动 damped harmonic oscillation](https://en.wikipedia.org/wiki/Harmonic_oscillator#Damped_harmonic_oscillator "阻尼谐振动 damped harmonic oscillation")的弹性模型生成一个动画值。它会在toValue值更新的同时跟踪当前的速度状态，以确保动画连贯。可以链式调用。(弹簧效果)

```javascript 
onPanResponderRelease: (e, {vx, vy}) => {
    Animated.spring(
        this.state.pan,
        {toValue: {x: 0, y: 0}}
    ).start();

}
```


### Animated.parallel()

            同时开始一个动画数组里的全部动画。默认情况下，如果有任何一个动画停止了，其余的也会被停止。可以通过stopTogether 选项设置为 false 来取消这种关联，格式如下： Animated.parallel(Animates\<Array>, \[conf\<Object>])：，conf参数格式：

```javascript 
this.parallelAnimated = Animated.parallel(
            [
                Animated.timing(
                    this.state.dogOpacityValue,
                    {
                        toValue: 1,
                        duration: 1000,
                    }
                ),

                Animated.timing(
                    this.state.dogACCValue,
                    {
                        toValue: 1,
                        duration: 2000,
                        easing: Easing.linear,
                    }
                ),
            ],
            {
                stopTogether: false
            }
        );
```


# 九：Animate.Value()/ValueXY()

[React Native Animated 动画详解 - 实例篇 (这篇就够了)*barnettyj的博客-CSDN博客 之前已经更新过Animated动画基础篇相关知识点,今天讲解Animated.Value,Animated.ValueXY以及相关动画库使用实例。学习完本篇相信大家对于动画库的基本使用应该是没有问题了。  <http://mp.weixin.qq.com/s>?* \_biz=MzA4OTc4MTM0OA==\&mid=2650358193\&idx=1\&sn=2f8bd640476df0659d99 <https://blog.csdn.net/sinat_17775997/article/details/60955657>](https://blog.csdn.net/sinat_17775997/article/details/60955657 "React Native Animated 动画详解 - 实例篇 (这篇就够了)_barnettyj的博客-CSDN博客 之前已经更新过Animated动画基础篇相关知识点,今天讲解Animated.Value,Animated.ValueXY以及相关动画库使用实例。学习完本篇相信大家对于动画库的基本使用应该是没有问题了。  http://mp.weixin.qq.com/s?__biz=MzA4OTc4MTM0OA==\&mid=2650358193\&idx=1\&sn=2f8bd640476df0659d99 https://blog.csdn.net/sinat_17775997/article/details/60955657")

## API

### 1.setValue()

修改new Animated.Value()的值

### 2.flattenOffset()

将偏移量合并到基值中，并将偏移量重置为零。该值的最终输出不变。

### 3.setoffset()

         设置应用于所设置值之上的偏移量，无论该值是通过setValue、动画或animate .event设置的。对于**补偿**像平移手势的开始这样的事情很有用。

```javascript 
this.state.pan.setOffset({
    x: this.state.pan.x._value,
    y: this.state.pan.y._value
});
```


### 4.注释：

- 无论是moveY相对于屏幕还是pageY相对于根元素都包括了顶部的statusBarHeight

### 5.interpolate

interpolate() 这个函数很强大，实现了数值大小、单位的映射转换，

```javascript 
const rotateZ = this.state.animatedValue.interpolate({

      inputRange: [0, 1],

      outputRange: ['0deg', '360deg']

  });

  const opacity = this.state.animatedValue.interpolate({

      inputRange: [0, 0.5, 1],

      outputRange: [0, 1, 0]

  });

  const rotateX = this.state.animatedValue.interpolate({

      inputRange: [0, 0.5, 1],

      outputRange: ['0deg', '180deg', '0deg']

  });

  const textSize = this.state.animatedValue.interpolate({

      inputRange: [0, 0.5, 1],

      outputRange: [18, 32, 18]

  });

  const marginLeft = this.state.animatedValue.interpolate({

      inputRange: [0, 0.5, 1],

      outputRange: [0, 200, 0]

  });
```


# 10：案例文档

## 基本api：

[react native实现拖拽/拖放  https://www.sheng00.com/2270.html](https://www.sheng00.com/2270.html "react native实现拖拽/拖放  https://www.sheng00.com/2270.html")

[https://blog.csdn.net/iostiannan/article/details/85095788](https://blog.csdn.net/iostiannan/article/details/85095788 "https://blog.csdn.net/iostiannan/article/details/85095788")

## 案例一：拖拽加吸边

1. 注意：在小圆的拖拽中要减去 statusBar的高度；44因为屏幕的横纵坐标是包括statusbar的
2. 结合两者可得出完美的拖拽小圆加吸附效果
   1. 把new Animate.ValueXY() ; 可以分开写；api不变；并设置初始值
   2. 最后松开的时候加上边界判断；并单独设置Top的值：
   3. 期间还有关于statusBar 和 bottom Bar的一些高度的获取
   4. 这下面是一个拖拽小圆外加吸边的效果；

```javascript 
state = {
  left:new Animated.Value(width-100),
  top:new Animated.Value(height-300)
}
this._panResponder = PanResponDer.create({
  onMoveShouldSetPanResponder:()=>true,
  onMoveShouldSetPanResponderCapture:()=>true,
  onPanResponderGrant:(e,g)=>{
    this.state.left.setOffset(this.state.left._value);
    this.state.top.setOffset(this.state.top._value);
    this.state.left.setValue(0)  ;
    this.state.top.setValue(0);
  },
  onPanResponderMove:Animated.event([
    null,{dx:this.state.left,dy:this.state.top}
  ]),
  onPanResponderRelease:(e,g)=>{
    let arr = [];
    this.state.top.flattenOffset();
    this.state.left.flattenOffset();
    let pagex = evt.nativeEvent.pageX;
    if(pagex<width/2){
      pagex = 0
    }else{
      pagex = width - toPX(96)
    }
    arr.push(
      Animated.spring(
          this.state.left,
          {
            toValue:pagex
          }
        )
    )
    if(evt.nativeEvent.pageY < statusBarHeight + toPX(96){
      arr.push(Animated.spring(
          this.state.top,{toValue:0}
        ))   
    }else if(evt.nativeEvent.pageY > height-statusBarHeight - 49 - toPX(96)){
        arr.push(Animated.spring(
          this.state.top,{toValue:height-300}
        ))
    }
    Animated.parallel(arr,{
        stopTogether:false
    }).start()
    
  }
  
})
```


## 案例一扩展

```javascript 
const screenWidth=Dimensions.get("window").width
  const screenHeight=Dimensions.get("window").height
  const { showFloatWindow } = useStore()

  const pan = useRef(new Animated.ValueXY()).current;

  const {
    height=300,
    width=parseInt(screenWidth*3/4),
  } = props



  const panResponder = useRef(PanResponder.create({
    onStartShouldSetPanResponder: () => true,
    onMoveShouldSetPanResponder: () => true,
    onPanResponderMove: Animated.event(
      [
        null,
        { dx: pan.x, dy: pan.y }
      ],
      {useNativeDriver: false}
    ),
    onPanResponderGrant: () => {
       pan.setOffset({
        x: transformX ?? pan.x._value,
        y: transformY ?? pan.y._value
      }); 
       pan.setValue({
        x: 0,
        y: 0
      });
     },
    onPanResponderRelease: (e,gestureState) => {
      const locationX = e.nativeEvent.locationX
      const locationY = e.nativeEvent.locationY
      const pageX = e.nativeEvent.pageX
      const pageY = e.nativeEvent.pageY
      pan.flattenOffset(); // 将偏移值到初始值；并将偏移值 归0；最终输出值不变
      transformX = undefined;
      transformY = undefined;
      if(locationX >= pageX){
        transformX = 0
      }
      if((pageX - locationX + width) >= screenWidth){
        transformX = parseInt(screenWidth-width)
      }
      if((pageY-locationY + height) >= screenHeight){
        transformY = parseInt(screenHeight-height)
      }
      if(pageY <= locationY){
        transformY = 0
      }
      Animated.spring(
        pan,
        {
          toValue:{x:transformX ?? pan.x._value,y:transformY ?? pan.y._value},
          useNativeDriver:true
        }
      ).start(({finished})=>{

      })
    },
  })).current;

  const handMessage = ()=>{

  }
  if(!showFloatWindow){
    return null
  }
  return <>
    <Animated.View
      style={[
        {
          transform: [{ translateX: pan.x }, { translateY: pan.y }],
          width: width,
          height: height,
          position:"absolute",
          zIndex: 10000,
        }
      ]}
      {...panResponder.panHandlers}
    >
      <WebView
        // ref={this.refView}
        allowUniversalAccessFromFileURLs={true}
        source={{
          // uri: 'http://192.168.43.14:3000/'
          uri:'https://reactnative.cn/docs/panresponder'
        }}
        mixedContentMode={'always'}
        originWhitelist={['*']}
        allowsInlineMediaPlayback={true}
        mediaPlaybackRequiresUserAction={false}
        onMessage={handMessage}
        style={{
          width:'100%',
          height:'100%'
        }}
      />
    </Animated.View>
  </>
```


## 案例二 ：滚动导致颜色渐变

```javascript 
 export class AnimatedBackground extends Component {
    state = {
        color: new Animated.Value(0),
        colorx:new Animated.Value(0)
    }

    onScrollX = (e) => {
        Animated.event([{
            nativeEvent: {
                contentOffset: {
                    x: this.state.colorx
                }
            }
        }])
    }
    onScroll = Animated.event([{
        nativeEvent: {
            contentOffset: {
                y: this.state.color
            }
        }
    }])
    
    render() {
        // colorX=是否是水平滑动
        return (
            <Animated.View style={{
                backgroundColor: this.props.colorX ? this.state.colorx.interpolate({
                    inputRange: this.props.colors.map((_, i) => (i) * c.fitPx(710)),
                    outputRange: this.props.colors
                }):this.state.color.interpolate({
                    inputRange: [0, isIphoneX ? 88 : 64],
                    outputRange: ['#F3474600', '#F34746FF']
                })
            }}>
                {this.props.children}
            </Animated.View>
        )
    }
}
```


## 进阶api

这篇笔记是**进阶一点**的写法：

[React Native 动画（Animated） 在上篇文章中介绍了 LayoutAnimation 的用法，本篇文章就来详细介绍一下 Animated 的用法。 Animated 简介 Animated 库用于创建更精细的... https://www.jianshu.com/p/7fd37d8ed138?from=singlemessage](https://www.jianshu.com/p/7fd37d8ed138?from=singlemessage "React Native 动画（Animated） 在上篇文章中介绍了 LayoutAnimation 的用法，本篇文章就来详细介绍一下 Animated 的用法。 Animated 简介 Animated 库用于创建更精细的... https://www.jianshu.com/p/7fd37d8ed138?from=singlemessage")

## 手势冲突

### 拖拽和touch 的冲突

- \*\* 所以可以得出 只要父组件的*****onStartShouldSetPanResponderCapture*****设置为true 子组件就都不会得到响应，\*\*
- **而**\*\***设置true或false已经没有关系了**，
- onStartShouldSetPanResponder 如果设置true的话，单机手势是不会成为响应者的，不会触发Grant和Release事件
- touchable 不能成为响应者；同理可得，不是所有的组件都能加手势系统
- 单机和移动的申请成为响应者 可以区分单机还是移动
- 成功的解决了scrollView 和 touch 的手势冲突问题，拖拽和点击的手势冲突

```javascript 
<ScrollView
  //移动的时候申请成为响应者
  onMoveShouldSetPanResponderCapture={()=>true}
  onMoveShouldSetPanResponder={()=>true}  
  //点击的时候不申请成为响应者，在捕获阶段不补货
  onStartShouldSetPanResponder={（）=> false}
  onStartShouldSetPanResponderCapture={()=>false}
>
    <TouchableWithoutFeedback
      onPress={
        //点击事件    
      }
    >
      <View
        //申请成为点击的响应者，底层冒泡，不要再TouchableWithoutFeedback上，没有用，加载View上，就可以执行press事件
        onStartShouldSetPanResponder={（）=> true}
        onStartShouldSetPanResponderCapture={()=>true}
      >
      
      </View>
    </TouchableWithoutFeedback>
  
</ScrollView>
```


[ReactNative 移动与点击手势冲突解决办法与拖动view的及时更新 - 程序员大本营 ReactNative 移动与点击手势冲突解决办法与拖动view的及时更新，程序员大本营，技术文章内容聚合第一站。 https://www.pianshen.com/article/7114173435/](https://www.pianshen.com/article/7114173435/ "ReactNative 移动与点击手势冲突解决办法与拖动view的及时更新 - 程序员大本营 ReactNative 移动与点击手势冲突解决办法与拖动view的及时更新，程序员大本营，技术文章内容聚合第一站。 https://www.pianshen.com/article/7114173435/")

**这个地方怎么判断都不灵敏（不是不生效是不怎么灵敏），在拖拽和点击事件判断，可以参考下上面文章；在移动的距离判断上加一个兼容，移动距离太小就当做是点击**

## 翻转动画：

```vue 
 import {
  View,
  Text,
  Image,
  Platform,
  StatusBar,
  AppState,
  Animated,
  Easing
} from 'react-native'
import React,{PureComponent} from 'react'
import {withRedux} from "modern/components/NavWrapper";
import { compose } from 'redux'
import StyleSheet,{ getThemeColor} from 'react-common/utils/styleSheet'
import { pop,pushRoute } from 'react-common/actions/routes'
import PageContainer from 'react-common/components/PageContainer'
import BackButton from 'react-common/components/BackButton'
import type { Callback } from 'react-common/types/langType'
import Button from 'react-common/components/Button'
import { getScreenWidth,getScreenHeight } from 'react-common/const/ui-common'
import _ from 'lodash'
import { safeParseJSON } from 'react-common/utils/stringify'
import { InternalSounds, playSound } from 'react-common/utils/nativeUtils/soundPlayUtils'
import { Toast } from 'react-common/utils/nativeUtils/toastUtils'
import extendLiftCycle from 'react-common/base/hoc/extendLifeCycle'
import withNotingRender from 'react-common/base/hoc/withNotingRender'
import PostSmsScanCamera from "sms/pages-merge/postSmsScan/_components/PostSmsScanCamera";
import deviceUtils from "react-common/utils/nativeUtils/deviceUtils";
import ModalAlertView from "react-common/components/ModalAlertView";
import { isActiveAction } from '../../../actions/scanCodeToSend/noPrintScanCodeToSend'
import normalPress from "modern/enhance/normalPress";
const RNView = withNotingRender(View)

interface ISPropsType  {
  pushTo:Callback,
  popTo:Callback,
  isActive:Callback,
}
interface ISStatesType  {
  flashMode:boolean,
  noInBackground:boolean,
  isShowCamera:boolean,
  isShowMask:boolean,
  startKb:string,
  secondKb:string,
  confirmActivation:boolean,
  animatedValue:any,
}
class NoprintScanPage extends PureComponent<ISPropsType,ISStatesType> {
  public scanView:any
  public frameView:any
  public toastDelayTime:number
  public preResulText:any
  public startCode:string =''
  public endCode:string =''
  public count:string =''
  constructor(props:ISPropsType){
    super(props)
    this.state={
      flashMode:false,
      noInBackground:true,
      isShowCamera:false,
      isShowMask:false,
      startKb:'',
      secondKb:'',
      confirmActivation:false,
      animatedValue: new Animated.Value(0),
    }
    this.toastDelayTime = Platform.OS === 'ios' ? 100 : 50
  }
  public startAnimate = ()=>{
      this.state.animatedValue.setValue(0);
      Animated.timing(
        this.state.animatedValue,
        {
          toValue: 1,
          duration: 500,
          easing: Easing.linear,
          useNativeDriver:true,
        }
      ).start(()=>{
        this.state.animatedValue.setValue(0);
      })
    Toast.showShortCenter('扫码成功，请扫描尾张进行激活')
  }

  public failSound = () => {
    _.delay(()=>{
      playSound(InternalSounds.failSound)
    },this.toastDelayTime)
  }
  public successSound = () => {
    _.delay(() => {
      playSound(InternalSounds.ding)
    }, this.toastDelayTime)
  }
  public wayBillDataHandle =  (scanCodeParams: object) => {
    const { startKb ,secondKb} = this.state
    const codeParams = safeParseJSON(scanCodeParams) || {}
    const data = _.trim(codeParams.value) || ''
    if(startKb && secondKb) return
    if (!data) {
      this.failSound()
      return
    }
    if (this.preResulText === data) {
      this.rePreview()
      return
    }
    this.preResulText = data
    if(!startKb) {
      this.startAnimate()
      return this.setState({startKb:data})
    }

    if(!secondKb){
      this.setState({secondKb:data},()=>{
        this.handleCallback()
      })
    }
  }
  public wayBillDataCallback = normalPress(this.wayBillDataHandle,1500)
  public handleCallback = ()=>{
    // 校验 是否已经被激活过了
    this.props.isActive({
      start:this.state.startKb,
      end:this.state.secondKb,
      callback:(res:any)=>{
        if(String(res.code) !== '0'){
          Toast.showShortCenter(res.msg || 'isActive Error')
          if(String(res.code) ==='500101'){
            this.preResulText =''
            this.setState({secondKb:''})
          }else{
            this.hideActivationModal()
          }
          return
        }
        const {has_active,start,end,count} = res.data || {}
        this.startCode = start
        this.endCode= end
        this.count = count
        if(String(has_active) === '1'){
          this.setState({confirmActivation:true})
        }else{
          this.continueActivation()
        }
      }
    })}
  public toggleFlashLight = () => {
    this.setState({ flashMode: !this.state.flashMode }, () => {
      this.scanView.setFlash(this.state.flashMode)
    })
    return Promise.resolve()
  }

  public rePreview = ()=>{
    if(this.scanView){
      this.scanView.restart()
    }
  }

  public whenRender = ()=> this.state.noInBackground && this.state.isShowCamera
  public render(){
    const {flashMode,isShowMask,startKb,confirmActivation} = this.state
    const rotateY = this.state.animatedValue.interpolate({
      inputRange: [0,0.5,1],
      outputRange: ['0deg','-90deg','-180deg']
    });

    return (
      <PageContainer
        title={`扫一扫`}
        left={<BackButton />}
        middleRight={
          <Button
            style={styles.flashView}
            onPress={this.toggleFlashLight}
          >
            <Image
              resizeMode={'cover'}
              source={!flashMode ?
                require('./images/flash_1.png') :
                require('./images/flash_2.png')}
            />
          </Button>
        }
        navBarStyle={styles.navBarStyle}
      >

        <Animated.View
          style={{
            flex:1,
            transform: [
              {rotateY},
            ]}}
        >
          <RNView
            whenRender={this.whenRender()}
            style={{ flex: 1 }}
            collapsable={false}
            replaceView={
              React.forwardRef((props, ref) => (
                <View style={styles.cameraBg} ref={ref}>
                  {props.children}
                </View>
              ))
            }
          >
            <PostSmsScanCamera
              ref={(el: any) => {
                if (el) {
                  this.scanView = el
                }
              }}
              style={styles.scanView}
              onNoFilterScanned={this.wayBillDataCallback}
              scanType={'camera'}
            />
          </RNView>
          <View style={styles.bottomView}>
            <View style={styles.stepView}>
              <Text style={[styles.stepNotice]}>第{startKb?'二':'一'}步</Text>
              <Text style={styles.descText}>{startKb?'选择尾张进行扫码激活':'请选择首张进行扫码激活'}</Text>
            </View>
            <View>
              <Text style={styles.descText}>{startKb?'每次激活单量<=400':'扫码激活首尾两个扫码寄贴纸的二维码，此时这一段扫码寄都会被激活，可交付使用'}</Text>
            </View>
          </View>
          <RNView
            whenRender={!isShowMask}
            style={styles.cameraMaskView}
          />
          <View style={{
            alignItems: 'center',
            position: 'absolute',
            paddingVertical: 1,
            bottom: 0,
            right: 0,
            width: getScreenWidth(),
          }}>
            <View
              style={{
                height: 1,
                width: getScreenWidth(),
                backgroundColor: '#000',
              }}
            />
          </View>
        </Animated.View>


        <ModalAlertView
          visible={confirmActivation}
          onCancel={this.hideActivationModal}
          content={`该号码段${this.startCode} - ${this.endCode}已被激活；重新激活系统将绑定新的信息，是否继续`}
          title={'温馨提示'}
          hiddenLine={true}
          button={[{
            text: '取消', onPress: this.hideActivationModal,textStyle:{color:'#696969'}
          }, {
            text: '继续激活',
            onPress: this.continueActivation,textStyle:{color:'#696969'}
          }]}
        />
      </PageContainer>
    )
  }
  public hideActivationModal =()=>{
    this.preResulText = ''
    this.state.animatedValue.setValue(0)
    this.setState({confirmActivation:false,startKb:'',secondKb:''})
  }
  public continueActivation = ()=>{
    const {startKb,secondKb } = this.state
    this.hideActivationModal()
    this.props.pushTo({
      page:'ScanExpressPrintTemplatePage',
      params:{
        pageFrom:'NoprintScanPage',
        NoprintScanPageData:{startKb:this.startCode,secondKb:this.endCode,count:this.count,startUrl:startKb,endUrl:secondKb}
      }
    })
  }
  public startScan=()=>{
    _.delay( ()=> {
      if (this.scanView) {
        if (this.frameView) {
          this.frameView._setGunViewMargin()
        }
        this.scanView.componentWillAppear()
        this.scanView.start()
        this.scanView.restart(false)
        _.delay(() => {
          this.scanView.changeMode('code')
        }, 500)
      }
      deviceUtils.setScreenLight({ isLight: true })
    }, 200)
  }
  public stopScan = () => {
    if (this.scanView) {
      this.scanView.setFlash(false)
      this.scanView.stop()
    }
    this.setState({
      isShowMask: false,
      isShowCamera: false,
    })
  }
  public delayShowCameraView = ()=>{
    _.delay(() => {
      this.setState({
        isShowCamera: true,
      })
    }, 100)

    _.delay(() => {
      this.setState({
        isShowMask: true,
      })
    }, 160)
  }
  public componentWillAppear(){
    this.setState({startKb:'', secondKb:'',})
    this.state.animatedValue.setValue(0)
    this.preResulText = ''
    StatusBar.setBackgroundColor('transparent')
    AppState.addEventListener('change', this.handleAppChange)
    this.startScan()
    this.delayShowCameraView()
  }
  public componentWillDisappear(){
    this.stopScan()
    AppState.removeEventListener('change', this.handleAppChange)
  }
  public handleAppChange = (currentAppState:string)=>{
    if (currentAppState === 'active') {
      this.setState({
        isShowMask: true,
        isShowCamera: true,
      })
    } else {
      this.stopScan()
    }
    this.setState({
      noInBackground: currentAppState === 'active',
    })
  }
}

const styles = StyleSheet.create({
  flashView: {
    paddingLeft: 20,
  },
  stepView:{
    flexDirection:'row',alignItems:'center',justifyContent:'center',marginBottom:8
  },
  bottomView:{
    height: 90,
    backgroundColor:'black',
    justifyContent:'center',
    paddingHorizontal:10,
  },
  descText:{
    fontSize:13,
    color:'#fff',
    textAlign:'center'
  },
  stepNotice:{
    fontSize:15,
    color:'#fff',
    width:60,
    height:24,
    backgroundColor:'#fa742b',
    textAlign:'center',
    borderRadius:4,
    marginRight:6,
    lineHeight:24,
  },
  navBarStyle:{
    backgroundColor: '#000000',
  },
  cameraMaskView: {
    backgroundColor: '#000000',
    position: 'absolute',
    left: 0,
    top: 0,
    bottom: 0,
    width: getScreenWidth(),
    height: getScreenHeight(),
  },
  scanView: {
    flex: 1,
    position: 'absolute',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    backgroundColor: 'black',
  },
})

function mapDisaptch(dispatch:any){
  return {
    pushTo: compose(dispatch, pushRoute),
    popTo: compose(dispatch, pop),
    isActive: compose(dispatch, isActiveAction),
  }
}
export default withRedux(null,mapDisaptch)(extendLiftCycle(NoprintScanPage))
```
