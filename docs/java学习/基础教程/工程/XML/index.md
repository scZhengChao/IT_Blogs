# XML

## 目录

- [什么是XML](#什么是XML)
- [XML作用](#XML作用)
- [小结](#小结)

* [ ] 能够说出xml的作用
* [ ] 能够使用Dom4j解析xml文件
* [ ] 能够使用XPath解析XML文件
* [ ] 利用反射、注解、动态代理、xml解析完成jdbc案例

#### 什么是XML

1. 英文：eXtensible Markup Language **可扩展的标记语言**，由各种标记(标签，元素)组成。
2. 可扩展：**所有的标签都是自定义的，可以随意扩展的**。如：\<abc>，<姓名>
3. 标记语言：**整个文档由各种标签组成**。清晰，数据结构化！
4. **XML是通用格式标准，全球所有的技术人员都知道这个东西**，都会按照XML的规范存储数据，交互数据！！

#### XML作用

作用：总体上来说，**就是为了存储维护数据的。**

1\) 数据交换：不同的\*\*计算机语言之间，不同的操作系统之间，不同的数据库之间，进行数据交换。 \*\*

&#x20;

![](./assets/image/image_ucEu45kLm0.png)

2\) 配置文件：在后期我们主要用于**各种框架的配置文件基本天天见。**

比如 连接池：c3p0-config.xml 日志：logback.xml

![](./assets/image/image_mi78LqHY9g.png)

#### 小结

1. xml是什么？
   - 可扩展的标记语言
     - 标记：标签。例：
   - xm**l文件是由N多个标签组成的**
2. 主要有哪两个作用？

- **存储数据**
- **配置文件（主流）**

[编写第1个XML文件](./编写第1个XML文件/index.md "编写第1个XML文件")

[XML的组成:声明和元素](./XML的组成-声明和元素/index.md "XML的组成:声明和元素")

[XML的组成：属性、注释和转义字符](./XML的组成：属性、注释和转义字符/index.md "XML的组成：属性、注释和转义字符")

[XML的组成：字符区(了解)](./XML的组成：字符区(了解)/index.md "XML的组成：字符区(了解)")

[XML约束：DTD约束](./XML约束：DTD约束/index.md "XML约束：DTD约束")

[XML约束：Schema约束](./XML约束：Schema约束/index.md "XML约束：Schema约束")
