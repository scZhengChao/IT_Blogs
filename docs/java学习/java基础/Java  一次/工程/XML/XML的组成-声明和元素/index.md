# XML的组成:声明和元素

## 目录

- [XML组成](#XML组成)
- [文档声明](#文档声明)
- [元素（标签、标记）](#元素标签标记)
- [小结](#小结)

#### XML组成

1. 声明
2. 元素(标签)
3. 属性
4. 注释
5. 转义字符【实体字符】
6. CDATA 字符区

#### 文档声明

```xml 
<?xml version="1.0" encoding="utf-8" ?>    固定格式

```


1. IDEA会自动提示。
2. 文档声明必须为\<?xml开头，以？>结束
3. 文档声明必须从文档的1行1列位置开始,==**必须在xml文档中的首行首列**==

   ![](./image/image_XB18HwCiBF.png)
4. 文档声明中常见的两个属性：
   - version：指定XML文档版本。必须属性，这里一般选择1.0；
   - encoding：指定当前文档的编码，可选属性，默认值是utf-8；

#### 元素（标签、标记）

```xml 
格式1:  <person> 标签体 </person>   有标签体的标签    （双标签）
格式2:  <person/>  没有标签体的标签 （空标签）         （单标签）

```


1. 元素是XML文档中最重要的组成部分；
2. 普通元素的结构由开始标签、元素体、结束标签组成。【格式1】
3. 元素体：**元素体可以是元素，也可以是文本**，例如：

```xml 
<person> 
   标签中可以包含另一个标签  
   <name>张三</name>  
</person>
```


1. 空元素：空元素只有标签，而没有结束标签，但元素必须自己闭合，例如：

```xml 
<sex/>

```


1. 元素命名
   - **区分大小写**
   - 不能**使用空格，不能使用冒号**
   - 不建**议以XML、xml、Xml开头**
   - 标签名**不能数字开头，可以有数字**
   - 可以**使用下划线****可以保持与Java命名标识符一样的规则**
2. \==**格式化良好的XML文档，有且仅有一个根元素**。==

错误演示：

元素没有结束

![](./image/image_AI7LnMbvt1.png)

元素大写小写不一致

![](./image/image_i88840eSeR.png)

xml中多个根

![](./image/image_aBrCt5WOGX.png)

### 小结

1. **声明有哪两个常用的属性？**
   - version
   - encoding
2. **一个XML有几个根元素？**
   - 有且只能有一个根元素
3. **XML标签命名不能有什么符号？**
   - 不能使用关键字xml、XML
   - 不能有空格、不能有冒号
   - 不能以数字作为开头
