# 代理

tunnel

[   https://www.npmjs.com/package/tunnel](https://www.npmjs.com/package/tunnel "   https://www.npmjs.com/package/tunnel")

needle

[   https://www.npmjs.com/package/needle](https://www.npmjs.com/package/needle "   https://www.npmjs.com/package/needle")

![](image_DaQ532u29N.png)

配合agent实现代理

或者

```typescript 
import { httpOverHttp, httpsOverHttp } from 'tunnel'
const httpsRxp = /^https:/
export const getRequestAgent = url => {
    let options
    if (proxy.enable && proxy.host) {
        options = {
            proxy: {
                host: proxy.host,
                port: proxy.port,
            },
        }
    } else if (proxy.envProxy) {
        options = {
            proxy: {
                host: proxy.envProxy.host,
                port: proxy.envProxy.port,
            },
        }
    }
    return options ? (httpsRxp.test(url) ? httpsOverHttp : httpOverHttp)(options) : undefined
}


agent: getRequestAgent(url),

```
