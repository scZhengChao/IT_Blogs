# 使用 ASAR 包

`Electron` 有两套 `API`. ：`Node AP`I 和 `Web API` 分别由`  Node.js  `和 `Chromium` 提供。 两套 API 都支持都支持从 `ASAR` 包内**读取文件.**

[Node API](<./Node API/index.md> "Node API")

[Web API](<./Web API/index.md> "Web API")

[将 ASAR 归档文件看作是一个普通文件](<./将 ASAR 归档文件看作是一个普通文件/index.md> "将 ASAR 归档文件看作是一个普通文件")
