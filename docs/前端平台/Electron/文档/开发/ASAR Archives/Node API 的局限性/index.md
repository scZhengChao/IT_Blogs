# Node API 的局限性

尽管我们已经尽了最大努力使得 ASAR 文件使用 Node API 时的应用尽可能的趋向于真实的目录结构，但仍有一些底层 Node API 我们无法保证其正常工作。

[档案文件是只读的](./档案文件是只读的/index.md "档案文件是只读的")

[工作目录不能设置为档案文件里的目录](./工作目录不能设置为档案文件里的目录/index.md "工作目录不能设置为档案文件里的目录")

[某些 API 需要额外解压档案包](<./某些 API 需要额外解压档案包/index.md> "某些 API 需要额外解压档案包")

[fs.stat 的不真实统计信息](<./fs.stat 的不真实统计信息/index.md> "fs.stat 的不真实统计信息")

[执行 ASAR 档案中的二进制文件](<./执行 ASAR 档案中的二进制文件/index.md> "执行 ASAR 档案中的二进制文件")

[向 ASAR 档案添加未打包的文件](<./向 ASAR 档案添加未打包的文件/index.md> "向 ASAR 档案添加未打包的文件")
