# 如何安装原生模块

有多种不同的方法来安装原生模块：

[为 Electron 安装并重新编译模块](<./为 Electron 安装并重新编译模块/index.md> "为 Electron 安装并重新编译模块")

[通过 npm 安装](<./通过 npm 安装/index.md> "通过 npm 安装")

[为 Electron 手动编译](<./为 Electron 手动编译/index.md> "为 Electron 手动编译")

[为Electron的自定义编译手动编译](./为Electron的自定义编译手动编译/index.md "为Electron的自定义编译手动编译")
