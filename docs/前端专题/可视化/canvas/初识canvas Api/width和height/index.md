# width和height

[canvas尺寸](./canvas尺寸/index.md "canvas尺寸")

[全重置画布​​](./全重置画布/index.md "全重置画布​​")

[如果canvas的width 和 canvas.style.width 不一致导致的后果](<./画布尺寸与CSS尺寸/index.md> "如果canvas的width 和 canvas.style.width 不一致导致的后果")

[如何改变canvas.width 或者height的尺寸，内容自适应](<./画布尺寸自适应/index.md> "如何改变canvas.width 或者height的尺寸，内容自适应")

[滚轮缩放导致canvas 出现的问题](<./滚轮缩放导致canvas 出现的问题/index.md> "滚轮缩放导致canvas 出现的问题")
